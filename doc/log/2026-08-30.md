# 2026-08-30

## Core/Offline 模型下载重试修复

- 根因：真实 WhisperKit 下载曾从 Hugging Face Xet CDN 返回 `NSURLErrorDomain Code=-1200` TLS 错误；同一固定 revision 重试后可完成，确认不是模型地址或清单缺失。
- 修复：新增可取消的有限传输重试策略；WhisperKit Hub 快照失败时重新获取元数据并复用 Hub 缓存，通用模型包逐文件失败时保留 Range 续传；取消、HTTP、校验和磁盘错误不自动吞掉。
- 测试：模型重试/永久失败/取消策略与通用模型包传输中断恢复测试通过；完整 `swift test --no-parallel` 通过 224 项 / 14 个 Suite；真实 `WOICE_RUN_REAL_WHISPERKIT=1 swift test --no-parallel --filter realWhisperKitModelPackDownloadsAndTranscribes` 下载、安装和本机转写通过。
- 发行包：`WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make release-adhoc` 通过，生成 `release/0.1.3-1-arm64-adhoc/`；Core/Offline 均为 arm64、ad hoc 严格验签和 DMG `hdiutil verify` 通过，Core 内置模型文件数为 0，Offline bundled 模型文件数为 22。
- 边界：本轮验证的是安装器真实下载/安装路径；当前桌面处于锁定状态，未将 UI 点击 Journey 冒充为已完成。用户可在解锁后用新包点击“下载并使用”复验。

## 0.1.3 build 2 Store 归档与公开包

- 版本：`CFBundleShortVersionString=0.1.3`、`CFBundleVersion=2`；Store Archive 已用本机 Apple Distribution 证书签名，`codesign --verify --deep --strict` 通过，Bundle ID 为 `com.water.woice`。
- 公开包：重新生成 `release/0.1.3-2-arm64-adhoc/`，Core/Offline 两个 arm64 DMG 的 ad hoc 签名、模型边界和 `hdiutil verify` 通过。
- TestFlight 前置：补建并导入本机 Mac Installer Distribution 证书，已用 `productbuild` + `productsign` 生成签名 `.pkg`；App Store Connect 上传认证仍待配置。

## 前端版本号显示

- 实现：工作台侧栏导航副标题动态显示 Bundle 版本和 Build（例如 `工作台 · v0.1.3 (Build 2)`）；设置 → 文件与隐私继续显示同一来源的版本字段。
- 边界：版本只读，来源为 `CFBundleShortVersionString` / `CFBundleVersion`，缺失时安全回退为“开发版”；未改变存储、模型或签名配置。
- 测试：新增版本格式与缺失字段回退测试；定向测试 2 项、完整 `make verify` 226 项、`make lint` 均通过。

## 模型下载取舍说明与 Store 清单门禁

- 实现：模型下载卡统一展示设备影响、产出质量和选择提示；Tiny（约 79 MB / 低资源 / 基础质量）、Qwen3-ASR 0.6B 4-bit（约 713 MB / 中资源 / 平衡预览）和 Large-v3（约 626 MB / 高资源 / 最高已验证）均可按场景选择。
- 下载：设置页 Tiny、Qwen3-ASR、Large-v3 共用现有 durable 下载、校验、原子安装和完成后切换逻辑；Large-v3 已从独立条目收口到共用模型卡。
- Store 边界：Store/TestFlight 只有在 Bundle 中存在已验证模型清单条目且含下载地址时才启用模型下载按钮；当前 0.1.3 build 2 Archive 的 Info.plist 没有 `WOICEModelCatalog*` 配置，因此当前 TestFlight/Store 构建不能实际下载 Large-v3，界面会显示“等待模型清单”，不会把失败点击伪装成可用。
- 测试：新增 2 项模型取舍文案测试；完整 `make verify` 228 项 Swift 测试、Store 条件 `make xcode-build-store` 和 `make lint` 通过。

## 签名模型 Catalog 与 TestFlight build 3 准备

- Catalog：固定 WhisperKit Tiny/Large-v3 revision，逐文件记录 SHA-256、大小、来源和跨仓库 HTTPS 下载地址；Ed25519 签名与 Store 内置公钥验签一致，生产 Catalog 测试通过。
- Store 配置：`Resources/Info.plist` 写入 Catalog URL/ID、可信公钥和 Catalog/模型下载 host allowlist；Store 下载只接受已验证 Catalog 条目，Tokenizer 与模型仓库分离时使用清单中的直接地址。
- 版本：`CFBundleShortVersionString=0.1.3`、`CFBundleVersion=3`；`make test` 通过 230 项 Swift 测试，`make xcode-build-store` 与 `make verify-app-store` 通过。
- 外部发布：Catalog 已推送到 `https://raw.githubusercontent.com/WaterDJiang/woice/main/Resources/ModelCatalog/model-catalog.json` 并回读一致；Apple Distribution 签名 Archive 与待上传 `Woice.pkg` 已导出。`xcodebuild -exportArchive` 上传因 `Failed to Use Accounts` 停止，需在 Xcode 登录开发者账号或提供 App Store Connect API Key 后继续，处理状态以 App Store Connect 页面为准。
