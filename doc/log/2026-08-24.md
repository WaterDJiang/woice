# 2026-08-24 执行日志

## 公开 GitHub 仓库与 ad hoc 预发布准备

- 用户确认：公开仓库采用 MIT；首批只支持 Apple Silicon（arm64）；保留 `Core`（无第三方模型）和 `Offline`（含已验证模型）两个 macOS 安装包；本次只生成 ad hoc 签名的预发布包。
- 范围：新增 README、LICENSE、公开仓库忽略规则和 `make release-adhoc`；不创建远程、提交或 GitHub Release，不执行 Developer ID 签名、公证或 Universal 构建。
- 实施：根目录新增 README、MIT LICENSE、`.gitignore`；`release-adhoc` 强制 ad hoc 签名、校验 Core/Offline 的签名、DMG 与 `arm64` 架构，并在 `release/<version>-<build>-arm64-adhoc/` 输出 DMG 与 SHA-256 清单。DMG 增加 `/Applications` 快捷方式。
- 验证：`make verify` 通过 190 项 Swift / 11 个 Suite、PI 6 项、MCP 2 项、文档/Harness、lint 和生产 Core 打包；两份 DMG 通过 `hdiutil verify`，两个 App 均为 `Signature=adhoc`、`TeamIdentifier=not set`、`arm64`。Offline 清单只包含 `com.woice.whisperkit.large-v3`，Core 模型列表为空。
- 产物：`release/0.1.0-1-arm64-adhoc/Woice-Core-0.1.0-1-arm64.dmg`（3.8 MB，SHA-256 `8f40dcbc5b27043ddfb2c2ee8acda2e276a5f07c3b81a7b995005a602b00baa8`）和 `Woice-Offline-0.1.0-1-arm64.dmg`（459 MB，SHA-256 `7d8f29d4a59c38ee48c3e482a54f8151def9cd03dd5739ff4682adf97aa80d5a`）已生成；二者与 `SHA256SUMS.txt` 都被 Git 忽略。
- 公开审计：敏感密钥模式扫描无匹配；拟公开的非忽略文件没有单个文件大于 1 MiB；构建目录、发行物、模型、录音、数据库、环境文件、证书和 Node/Python 缓存均命中 `.gitignore`。本地 Git 仓库已初始化为 `main`，未创建提交、远程或 GitHub Release。

## 双轨会议可靠合并转写

- 用户真实测试发现：会议转写会只保留麦克风或电脑声音，未形成完整会议原文。
- 证据：正常 `meetingMix` 文件包含两路信号，但 49 秒真实合成音频仅得到“OK 走”；最近异常恢复素材同时存在 WAV/CAF，却只创建了 `microphone` 任务。
- 根因一：默认先混音再执行一次 Whisper，重叠说话时模型会竞争性漏轨。
- 根因二：异常恢复只恢复麦克风任务，不重建会议回放，也不恢复双轨转写语义。
- 修复：新设置和旧设置迁移默认使用“完整会议”；麦克风、电脑声音分别进入设置页选定的模型，系统轨先标准化为 WAV，结果按录音偏移和来源合并；`meetingMix` 只用于回放。
- 修复：历史 `standardMix` 双轨素材在用户重转写时升级为双轨任务，旧 Transcript Artifact 和原始音频不覆盖；异常恢复重建 meetingMix 并采用双轨语义。
- 验证：频谱断言确认 meetingMix 同时包含 440 Hz 麦克风与 880 Hz 系统输入；历史迁移、异常恢复、双请求、来源标签、系统轨标准化和原始字节不变专项测试通过；`make verify` 通过 186 项 Swift / 11 个 Suite、PI 6 项、MCP 2 项、文档/Harness、lint、生产构建与打包。
- 安装：同一 Apple Development 身份生成 A `2026082401` → B `2026082402` 并覆盖安装；Bundle ID `com.woice.app`、Team ID `C27W5WB6R8`、严格签名、单实例和“Woice 工作台”启动通过。证据 `/private/tmp/woice-stable-ab-20260824-dual-track-reliable`；未调用 `tccutil reset`。
- 运行态：设置迁移结果为 `meetingTranscriptionMode=sourceSeparated`、`meetingTranscriptionStrategyVersion=1`、`asrConfiguration.selection=onDevice`；修复没有切换到 HTTP/端口路线。
- 待验收：新安装包需由用户用麦克风与视频/会议的不同句子复验最终原文同时包含两路；此前手动通过继续证明双轨采集，不再证明旧版单次混音转写完整。

## 原文与声音来源展示分离

- 产品修正：纯文本原文只显示说话内容，不插入“我的麦克风/电脑声音”前缀；声音来源只保留在时间戳片段 UI 和 JSON `sourceTrack`。
- 历史兼容：Build `2026082402` 已写入的来源前缀不会原位覆盖；启动时从结构化片段创建一个干净的新 Transcript Artifact，旧版本继续保留在版本历史。
- 验证：新增历史前缀迁移测试；新双轨转写断言原文不含来源词，同时结构化片段仍包含 `microphone/systemAudio`；`make verify` 通过 187 项 Swift / 11 个 Suite、PI 6 项、MCP 2 项及全部文档、Harness、lint、生产构建和打包门禁。
- 安装：同一 Apple Development 身份生成 A `2026082403` → B `2026082404` 并覆盖安装；严格签名、单实例和“Woice 工作台”启动通过。证据 `/private/tmp/woice-stable-ab-20260824-plain-transcript`；未调用 `tccutil reset`。
- 运行态：启动迁移后，当前 `sourceSeparated` 原文中来源前缀计数为 0；保留 `microphone/systemAudio` 的结构化片段共 22 条。

## CLI 默认文字交付与素材废纸篓

- 产品修正：CLI Context Package 默认只包含规范化原文和任务说明，不再检查、复制或发送麦克风 WAV；派发 UI、权限摘要和审计 `dataTypes` 同步只声明文字。
- 删除交互：素材侧栏改为原生 `List`；尾部侧滑（禁止 full swipe）、右键菜单、选中后的删除按钮和 Delete 键进入同一确认框。
- 可恢复边界：删除先把 `record.json` 与 Recording 相关文件装入隔离目录，再提交索引并整体移到 macOS 废纸篓；索引持久化或废纸篓失败会恢复文件与 Recording。
- 验证：`make verify` 通过 190 项 Swift / 11 个 Suite、PI 6 项、MCP 2 项及文档、Harness、lint、生产构建和打包；新增文字独立派发、删除成功和删除回滚测试。
- 安装：同一 Apple Development 身份生成 A `2026082405` → B `2026082406` 并覆盖安装；严格签名、单实例和“Woice 工作台”启动通过。证据 `/private/tmp/woice-stable-ab-20260824-text-only-trash`；未调用 `tccutil reset`。
- 真实界面：AX 树确认每个素材行暴露“移到废纸篓”，选中前删除按钮禁用、选中后启用；确认框明确原音频、原文和相关本机文件进入 macOS 废纸篓。只读截图保存在 `/private/tmp/woice-material-list-audit/`，检查后取消确认，未删除用户素材。
