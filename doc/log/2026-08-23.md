# 2026-08-23

## 处理任务状态投影与 VoiceOver 一致性复验

- 根因：任务行视觉已使用 `activeTask`，但侧栏、处理列表和可继续任务的 VoiceOver 标签仍读取聚合的 `materialStatus`；旧失败任务与当前运行任务并存时，辅助技术会读到错误状态。
- 修复：`WorkspaceView` 的侧栏、处理列表和可继续任务统一使用 `ProcessingTaskProjection.activeTask/resumableTask`；可见文本、图标和无障碍标签来自同一持久化任务事实，不改写 RecordingRecord、Job 或 Artifact。
- 测试：`ProcessingTaskProjectionTests` 定向 5 项通过；`make acceptance-workspace-sidebar`、`WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window`、`WOICE_RUN_ACCESSIBILITY_JOURNEY=1 WOICE_RUN_KEYBOARD_JOURNEY=1 make acceptance-accessibility-runtime` 通过；最新 `make verify` 通过 183 项 Swift 测试 / 11 个 Suite、PI 6 项、MCP 2 项。
- 稳定包：Apple Development `Apple Development: jianglewis@126.com (23RRB32DPL)` 生成 A `2026082331` → B `2026082332`，证据目录 `/private/tmp/woice-stable-ab-20260823-task-a11y-projection`；B 已覆盖安装到 `/Applications/Woice.app`，严格签名和权限声明门禁通过。本轮未重置 TCC，也未把声明比较写成新的 TCC 授权实测。
- 同一 B 包的桌面边界：正常导入 `/private/tmp/woice-media-import-success-build2332`、损坏视频 `/private/tmp/woice-media-import-failure-build2332`、无音轨视频 `/private/tmp/woice-media-import-no-audio-build2332` 均通过，失败场景无 `.source.*`、`.wav` 或 `.m4a` 半成品。
- 剩余：真实用户长文件/第三方 Provider 失败重试、逐项 TCC 撤销重授权、完整 VoiceOver/视觉矩阵、长时稳定性、Developer ID/公证/生产 Catalog 和真实 Agent 入站仍未关闭；双轨系统会议转写按用户手动验收记为通过。

## 音视频导入失败语义与桌面边界复验

- 根因：损坏视频在 `AVURLAsset.loadTracks` 阶段抛出的容器读取错误被外层泛化为“原始文件保存失败”，用户无法判断是文件损坏还是保存目录有问题。
- 修复：`MediaImportService` 将视频容器/音轨读取失败稳定归类为“视频音轨读取失败”，文案不泄露本机路径；原件、派生 WAV 和临时提取文件仍在所有失败分支清理。
- 测试：`MediaImportTests` 新增空文件、损坏视频错误分类和无半成品回归；完整 `make verify` 通过 183 项 Swift 测试 / 11 个 Suite、PI 6 项、MCP 2 项。
- 桌面证据：稳定签名 A `2026082329` → B `2026082330` 已覆盖安装到 `/Applications/Woice.app`，证据目录 `/private/tmp/woice-stable-ab-20260823-media-import-failure`；正常 `Funk.aiff` 导入/Fixture 转写证据 `/private/tmp/woice-media-import-success-build2330`，损坏视频证据 `/private/tmp/woice-media-import-failure-build2330`，无音轨视频证据 `/private/tmp/woice-media-import-no-audio-build2330`。三条 Journey 均使用隔离存储，失败场景未留下 `.source.*`、`.wav` 或 `.m4a` 半成品。
- 运行时：Build B 启动/关闭/重开、AX 树和 `⌘2 -> 处理任务` Journey 通过；未调用 `tccutil reset`，用户真实长文件、真实 Provider 失败重试、逐项 TCC 重授权、视觉/长时矩阵和正式发行仍保留在剩余清单。

## 可继续任务状态投影与稳定签名复验

- 根因：处理任务页按“任意任务为排队/失败/中断”展示“可继续”，旧失败片段会在主转写已运行时制造重复继续入口；`resumeProcessing` 也直接取第一条排队任务，和用户看到的活动任务可能不一致。
- 修复：`ProcessingTaskProjection.resumableTask` 只接受活动任务的排队、失败或中断状态；工作台入口与 `AppState.resumeProcessing` 共用该投影，运行中、等待确认、等待模型和已完成任务不会显示重复继续动作。
- 测试：新增“运行中任务隐藏旧失败继续入口”和“活动排队任务仍可继续”2 项回归测试；完整 `make verify` 通过 181 项 Swift 测试 / 11 个 Suite。
- 稳定包：Apple Development `Apple Development: jianglewis@126.com (23RRB32DPL)` 生成 A `2026082327` / B `2026082328`，证据目录 `/private/tmp/woice-stable-ab-20260823-resumable-projection`；B 已覆盖安装到 `/Applications/Woice.app`，未重置 TCC，覆盖前包已保留。严格签名、启动/关闭/重开、AX 树和 `⌘2 -> 处理任务` Journey 通过。
- 未关闭边界保持不变：真实用户长文件/Provider、逐项 TCC 重新授权、完整 VoiceOver/视觉矩阵、长时稳定性、Developer ID/公证、生产 Catalog 和 CLI Beta 仍未完成；双轨系统会议转写按用户手动验收记为通过。

## 处理任务活动状态投影统一与稳定签名复验

- 根因：导入页已修复状态投影后，处理任务工作区仍使用 `record.processingTasks.last`。当旧记录同时存在主转写、分段转写或 Markdown 任务时，数组末项仍可能覆盖真正的运行中/等待模型事实。
- 修复：新增 `ProcessingTaskProjection`，按运行中、等待确认、等待模型、排队、失败/中断/取消、完成的固定优先级，再按 `updatedAt`、任务类型和数组位置决胜；导入页和处理任务列表共用该只读投影，不改写 RecordingRecord、Job 或 Artifact。
- 测试：新增 3 项活动任务投影测试，覆盖“运行中主任务优先于排队片段”“等待模型优先于更新失败任务”“转写完成后仍显示运行中的 Markdown 任务”；导入闭环 7 项测试通过。
- 稳定包：Apple Development `Apple Development: jianglewis@126.com (23RRB32DPL)` 生成 A `2026082325` / B `2026082326`，证据目录 `/private/tmp/woice-stable-ab-20260823-task-projection`；B 已覆盖安装到 `/Applications/Woice.app`，未重置 TCC，覆盖前包已保留。`com.woice.app`、Team `C27W5WB6R8`、CDHash `c397f1d811d9a3595164e4c8a624bc82b2b15a76`、严格签名校验通过。
- 运行时：最新 Build B 的启动/关闭/重开、单实例、AX 树和 `⌘2 -> 处理任务` Journey 通过。
- 最新 `make verify` 通过：179 项 Swift 测试 / 11 个 Suite、PI 6 项、MCP 2 项、文档/Harness、lint、生产构建和打包均通过。
- 未关闭边界保持不变：真实用户长文件/Provider、逐项 TCC 重新授权、完整 VoiceOver/视觉矩阵、长时稳定性、Developer ID/公证、生产 Catalog 和 CLI Beta 仍未完成；双轨系统会议转写按用户手动验收记为通过。

## 导入页转写状态投影修复与稳定签名复验

- 根因：`MediaImportSheet` 原先用数组最后一项驱动按钮/说明，而素材状态卡会汇总所有转写任务；旧记录同时存在运行中的主任务和排队的片段任务时，界面会出现“正在转写”与“点击转文字”相互矛盾。
- 修复：新增活动转写任务投影，按运行中、等待确认、等待模型、排队及失败状态排序；状态图标、标签、说明、主按钮标题和禁用态统一使用同一任务事实。新增旧记录主任务/片段任务回归测试。
- 稳定包：Apple Development `Apple Development: jianglewis@126.com (23RRB32DPL)` 生成 A `2026082323` / B `2026082324`，证据目录 `/private/tmp/woice-stable-ab-20260823-media-sheet-fix`；B 已覆盖安装到 `/Applications/Woice.app`，保留覆盖前备份。`com.woice.app`、Team `C27W5WB6R8`、严格签名校验、启动和工作台 AX 状态通过。
- 最新 `make verify` 通过：176 项 Swift 测试 / 10 个 Suite、PI 6 项、MCP 2 项、文档/Harness、lint、生产构建和打包均通过。
- 发行边界：当前机器仅有 Apple Development 身份；`xcrun notarytool history` 因未配置凭据退出，Developer ID、公证、生产 Catalog 和干净账户安装仍未完成。双轨系统会议转写继续按用户手动验收结论记为通过，不重复执行。

## 当前计划缺口复跑与双轨人工验收边界

- 修正 `LoopbackASRHTTPTests` 测试夹具：使用自定义 Endpoint 的场景现在显式设置 `asrProviderSelection = .external`，不再依赖旧的残留地址隐式切换；产品路由逻辑保持设置单一真相源。
- 当前 Mac 复跑通过：`acceptance-core`、`acceptance-meeting`、`acceptance-meeting-transcription`、`acceptance-interruption`、`acceptance-settings`、`acceptance-recovery`、`acceptance-media-import-transcription`、`acceptance-permission-continuity`、`acceptance-workspace-sidebar`、`acceptance-local-provider`、`acceptance-catalog`、`acceptance-whisperkit` 和 `acceptance-offline-model`。
- 真实运行证据包括：麦克风录音到本机 WhisperKit 文字、系统声音 CAF/meetingMix、模型库存/Offline DMG 校验、启动重开/单实例/工作台 AX 和设置/恢复契约；`WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window` 与 `WOICE_RUN_ACCESSIBILITY_JOURNEY=1 WOICE_RUN_KEYBOARD_JOURNEY=1 make acceptance-accessibility-runtime` 通过。
- 隔离桌面导入 Journey 以 `/System/Library/Sounds/Funk.aiff` 和合成 `.mov` Fixture 各复跑一次，均确认原件与派生 WAV/视频音轨提取/Fixture 转写持久化；两次临时存储根目录均由脚本退出清理，未写入正式素材库。
- 最终 `make verify` 通过：文档/Harness、PI 6 项、MCP 2 项、175 项 Swift 测试（10 个套件）、lint、生产构建和打包均通过。
- 双轨系统会议转写按用户指示由用户承担手动验收并记为通过；Codex 不再重复执行这项手动路径。自动契约与 QuickTime 声源脚本证据仍保留，真实用户长文件、第三方 Provider 失败重试、完整视觉/无障碍和发行关闭条件仍是剩余清单。

## 菜单栏录音状态与快捷键动态文案修复

- 修复菜单栏录音时长、麦克风状态和快捷键候选可用提示被当作字面量占位文本的问题；运行时文本与 VoiceOver 值现在均使用实际状态插值。
- `acceptance-launch-window` 与 `acceptance-workspace-sidebar` 增加静态契约检查，防止该类回归；`make lint`、`make test`（173 项 Swift / 10 个 Suite）和 `make verify` 通过，完整输出保存在 `/private/tmp/woice-verify-menu-label-fix.log`。
- 使用稳定签名 Apple Development `Apple Development: jianglewis@126.com (23RRB32DPL)` 生成 A `2026082317` / B `2026082318`，证据目录 `/private/tmp/woice-stable-ab-20260823-menu-label-fix`；随后已完成 A→B 覆盖安装，当前安装包为 B。
- 追加复跑 `make acceptance-permission-continuity acceptance-workspace-sidebar acceptance-launch-window acceptance-accessibility-runtime`：14 项权限契约测试、工作区/启动窗口/无障碍静态契约全部通过；运行时 AX、VoiceOver、高对比和 TCC 仍保留为人工验收。
- 逐项撤销/重新授权、VoiceOver/视觉矩阵、真实文件导入和长时稳定性仍不能写成已完成；A→B 已有权限连续性通过，后续按手册继续执行。

## 稳定签名 A→B TCC 覆盖安装实测

- Mac 解锁后按稳定签名流程正常退出旧实例，覆盖安装 A `2026082317`，启动后工作台窗口可见；设置页显示“系统音频能力可用”（2 个显示器）、“麦克风已就绪”和“已允许粘贴到当前应用”。
- 未重置 TCC，随后覆盖安装 B `2026082318`；B 启动、工作台、麦克风、系统音频和粘贴授权状态均保持，未出现重复授权提示。A/B 均为 `com.woice.app`、Team `C27W5WB6R8`，严格签名门禁通过。
- 截图证据：[A 设置页](../audit/2026-08-23-menubar-settings-shortcut/08-stable-a-settings-system-audio-ready.jpeg)、[B 设置页](../audit/2026-08-23-menubar-settings-shortcut/09-stable-b-settings-system-audio-ready.jpeg)；脚本证据仍在 `/private/tmp/woice-stable-ab-20260823-menu-label-fix`，安装前包已保留。
- 这关闭了“稳定签名 A→B 覆盖安装后权限状态丢失”的 P0 项；真实系统声音录制、真实视频/会议应用和长会恢复仍未关闭。

## Build B 启动/AX Journey 前台脚本修复

- Build B 首次真实启动 Journey 发现验收脚本在关闭工作台前没有将 System Events 进程置前，设置页可见但 `⌘W` 未生效；这是真实验收脚本缺陷，不是窗口控制器结论。
- `scripts/acceptance_launch_window.sh` 在关闭窗口前显式 `set frontmost to true`，并通过 `zsh -n` 语法检查。
- 修复后 `WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window` 通过（单进程、关闭、重开、窗口标题）；`WOICE_RUN_ACCESSIBILITY_JOURNEY=1 WOICE_RUN_KEYBOARD_JOURNEY=1 make acceptance-accessibility-runtime` 通过（AX 树、⌘2 → 处理任务）。

## Build B 真实桌面导入与系统音频复跑

- 关闭稳定 Build B 后，以 `/System/Library/Sounds/Funk.aiff` 作为非敏感系统 Fixture，执行 `WOICE_RUN_MEDIA_IMPORT_JOURNEY=1 make acceptance-media-import-desktop`，真实工作台导入→原件保存→派生 WAV→Fixture 转写通过。
- 保留证据目录：`/private/tmp/woice-media-import-buildB.3vzLAP`；`recordings.json` 显示 `sourceKind=importedAudio`、转写“桌面导入验收素材”、时长 `2.1634375s`；原件 SHA-256 与系统源一致，派生文件为可读取 RIFF/WAVE。
- `make acceptance-meeting` 在已解锁桌面通过（15.458s）：QuickTime 可见播放源的系统声音、CAF 和 `meetingMix` 双轨编排均有证据；未录制用户麦克风。
- 真实用户文件、真实会议应用、多窗口/全桌面差异、长会/睡眠/崩溃恢复和第三方 Provider 仍未宣称完成。
- 随后用系统声音合成的非敏感 QuickTime 视频 Fixture `/private/tmp/woice-import-video-20260823.mov` 复跑同一 Journey，通过 `sourceKind=importedVideo`、视频原件 SHA-256 与源一致、音轨提取临时文件和派生 WAV 持久化；证据目录 `/private/tmp/woice-media-import-video-buildB.87z9GZ`。

## Build B 稍后处理重启恢复 Journey

- 在现有工作台确认卡上只点击“稍后处理”，未点击“确认并开始”；任务从外发确认卡退出后，退出并重开稳定 Build B。
- 重启后的“处理任务”工作区显示“可继续的任务”和“继续处理”入口；点击其中一个继续入口后，确认卡重新出现，仍明确写出“录音素材已安全保存在本机”，没有自动外发。
- 截图证据：[重启后可继续任务](../audit/2026-08-23-menubar-settings-shortcut/10-after-restart-queued-tasks.jpeg)、[继续入口确认卡](../audit/2026-08-23-menubar-settings-shortcut/11-restarted-external-confirmation-card.jpeg)。这关闭了 MSS-07R 的“稍后处理/重启后丢失”项；真实 Provider 完成和云端重启后继续发送仍未验证。

## 权威路线图状态同步

- 将 M2-08a/b/d/e-i 和录音产品升级计划中的旧“进行中/默认模型未完成/搜索未完成”表述同步为当前代码与自动门禁事实；跨版本真实迁移、生产 Catalog、真实会议准确率、桌面视觉/TCC/长时矩阵、Agent 入站和正式发行仍明确保留为未完成。
- `make docs-check`、`make harness-check` 通过；没有改变运行时代码或用户数据。

## 专项规格状态与当前证据一致性复核

- 同步菜单栏/MSS、后台分段转写、系统音频窗口回退、ProcessingTask 配置快照、Catalog 多文件下载、本机 ASR 预设、模型 Fixture、Context Package 8 份规格的状态头。
- 已完成的代码/契约门禁不再标为“实施中”；真实长会、TCC/视觉、生产 Catalog、跨版本迁移和外部 Agent 入站等未完成边界继续显式保留。
- `make docs-check`、`make harness-check` 通过；未改变运行时代码或用户数据。

## 工作台外部处理确认卡无障碍插值修复

- 修复 `WorkspaceExternalProcessingCard` 把 `request.confirmationTitle` 当成字面量朗读的问题，改为运行时插值，VoiceOver 能读到实际处理目标。
- `acceptance-workspace-sidebar` 增加该字符串契约检查；`make docs-check`、`make harness-check`、`make lint`、`make test`、`make verify` 通过（173 项 Swift / 10 个 Suite）。
- 当时生成稳定签名候选 A `2026082315` / B `2026082316`，证据目录 `/private/tmp/woice-stable-ab-20260823-a11y-fix`；该候选已被后续动态文案修复候选 `2026082317→2026082318`（`/private/tmp/woice-stable-ab-20260823-menu-label-fix`）替代，均未覆盖安装当前运行中的 Woice。
- 真实截图、VoiceOver、高对比和 Mac GUI 复验仍需解锁当前 Mac 后执行；未修改 TCC 或强制关闭当前 Woice 进程。

## 当前计划进度复核与新增门禁结果

- 重新核对当前路线图、WPC 工作区/权限计划和 MSS-07R 计划；新增内容已归位，前端 CLI 继续标记为 `Beta` 并保持最后优先级。
- 本轮自动验收通过：`make acceptance-core`、`acceptance-meeting`、`acceptance-meeting-transcription`、`acceptance-interruption`、`acceptance-settings`、`acceptance-material`、`acceptance-recovery`、`acceptance-local-provider`、`acceptance-catalog`、`acceptance-media-import-transcription`、`acceptance-permission-continuity`、`acceptance-workspace-sidebar`、`acceptance-agent-outbound`、`acceptance-agent-inbound`；外部 Agent synthetic smoke 仍为 opt-in，未冒充真实外部连接完成。
- 当时安装包为稳定签名 Build `2026082312`，证据目录 `/private/tmp/woice-stable-ab-20260823-mss07r-v2`；后续已由 `2026082317→2026082318` 覆盖安装证据替代。
- 当时 `acceptance-media-import-desktop` 因已有 Woice 进程运行而安全退出，未强制结束用户进程；后续已在 Build B 使用音频和视频 Fixture 重跑通过。
- 当时结论为核心代码已完成、自动门禁通过；当前真实用户素材、正式发行和 CLI Beta 清单仍未关闭。详见 `doc/plan/2026-08-23-plan-progress-review.md`。

## MSS-07R 代码收口与稳定 B 重打包

- 完成 MSS-07R：新增 `LocalASRTrustSnapshot` 和 `local-asr-trust.json`，健康检查事实只保存规范化 Endpoint、模型、语言/时间戳和不含密钥的摘要；服务、模型或配置变化会失效，API Key 不进入该文件。
- 移除 `MenuBarPopover` 的转写模态 Alert；需要外发的确认迁入唯一工作台，菜单栏只显示“等待你确认”和“打开工作台”。“稍后处理”保留为 `queued + authorizationRequired`，工作台提供“继续处理”。
- 增加主转写与声音片段任务 guard、历史任务修复和启动恢复；同一素材主转写等待/运行时不再追加竞争片段任务。
- 自动验证：`make docs-check`、`make harness-check`、`make lint`、`make verify` 通过；最新 Swift `173 tests / 10 suites`，PI `6`、MCP `2` 全部通过。新增信任摘要持久化/失效、主/片段去重和重启恢复测试。
- 稳定包：使用 `Apple Development: jianglewis@126.com (23RRB32DPL)` 生成 A `2026082311` / B `2026082312`，证据目录 `/private/tmp/woice-stable-ab-20260823-mss07r-v2`；B 已覆盖安装到 `/Applications/Woice.app`，保留安装前备份。`codesign --verify --deep --strict` 与 Bundle ID/Team ID/权限声明门禁通过。
- 运行时门禁：`WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window` 通过（`process=1 window=Woice 工作台`）；`WOICE_RUN_ACCESSIBILITY_JOURNEY=1 make acceptance-accessibility-runtime` 通过。TCC A/B 授权、真实双轨录音、云端“稍后处理 -> 重启 -> 继续”仍待用户按手册验收；未调用 `tccutil reset`。

## 真实录音 Journey 重开 MSS-07R

- 新截图虽然已把输入写成“会议完整回放”，仍在菜单栏以模态 Alert 要求向 `127.0.0.1`“继续发送”，遮挡四动作并打断录音停止后的默认转写。
- 根因定位为 `verifiedLocalASREndpoints` 只存在当前 `AppState` 内存中：健康检查成功后本次进程可自动处理，但 App 重启或覆盖安装后验证事实丢失，同一本机服务再次逐条确认。
- “稍后处理”当前调用 `dismissExternalProcessing()`，把任务状态写成 `cancelled`，不是可恢复的稍后处理；文案与事实不一致。
- 最新记录同时存在等待确认的会议完整回放任务和声音片段任务，说明主转写等待时仍能追加竞争处理，可能造成连续确认和状态混乱。
- 计划新增 MSS-07R：持久化不含凭据的 loopback 授权摘要；本机转写不弹菜单栏 Alert；云端确认进入工作台；稍后处理改为可恢复状态；同一素材只允许一个活动主转写策略。
- 证据保存到 `doc/audit/2026-08-23-menubar-settings-shortcut/07-menubar-transcription-interruption.png`。本轮只读检查并更新计划、索引和日志，没有确认发送、修改用户任务或实现修复。

## 菜单栏、设置、快捷键与 Dock 图标精简优化实施

- 按 `doc/plan/2026-08-23-menubar-settings-shortcut-optimization.md` 完成 MSS-00～MSS-07 代码切片，并新增 `specs/2026-08-23-menubar-settings-shortcut-optimization.md` 作为实施契约。
- 菜单栏改为四动作命令面板：开始/结束录音、进入工作台、设置、退出；Popover 初始尺寸由 `fittingSize` 驱动并限制为 190～420 pt，录音状态保留真实时长、输入电平和电脑声音状态。
- 设置默认层收起实现术语，高级层承载模型下载、服务连接、诊断和兼容参数；分区独立保存与密钥按字段访问边界保持。
- `RecordingShortcut` 从固定枚举迁移为键码 + 修饰键值，兼容旧字符串；新增录入控件、系统保留校验、Carbon 探针和候选先注册/失败回滚。
- 移除 `NSApplication.applicationIconImage` 原始 PNG 覆盖路径，Bundle AppIcon 成为系统图标唯一来源；Popover 品牌预览仍使用 PNG。
- 录音详情统一为“我的声音（麦克风）/电脑声音/会议完整回放/转写分析”，原始/合成 Badge 与技术详情分层；合成失败不静默回退到麦克风，健康检查通过的 loopback 服务可自动继续转写，历史 meetingMix 任务启动时兼容修复和去重。
- 自动验证：`swift build`、`swift test`（169 项/10 个 Suite）、`swift format lint` 已通过；真实 Popover 多屏锚点、跨应用快捷键冲突、Dock/Finder 圆角、VoiceOver/TCC、真实录音和七状态会议 Journey 留给用户手动验收。

## 合并音频默认转写补入精简优化计划

- 用户截图中的“发送录音并转写”来自统一外部处理弹窗；它固定使用“发送/仅保存录音”，没有说明实际转写输入、处理位置和录音已经安全保存。
- 只读检查当前记录字段确认：存在一条已生成 `meetingMix`、标准模式任务仍停在等待确认且没有原文的记录；当前设置选择 `127.0.0.1` 本机 OpenAI-compatible 服务，因此阻塞来自本机服务复用外发确认流程。
- 另一个最新记录的电脑声音文件峰值为 0，因此没有生成合并音频，任务正确回退为麦克风；现有界面没有说明“未录到电脑声音，本次仅转写我的声音”，造成用户误以为合并路由错误。
- 优化计划冻结新顺序：双轨有效时默认只转写会议完整回放；内置模型和已显式选择、健康检查通过的 loopback 本机服务自动继续；未验证本机服务首次确认；局域网/云端仍在实际外发前确认。
- 通用弹窗改为来源与处理位置驱动；取消动作改为“稍后处理”。补充历史 `meetingMix` 来源缺失修复、同幂等键去重、重启恢复、合成失败禁止静默回退和 AC-MSS-025～032。
- 证据保存到 `doc/audit/2026-08-23-menubar-settings-shortcut/06-meeting-mix-transcription-confirmation.png`；本轮没有发送音频、执行真实 Provider、修改用户记录或实现代码。

## 录音来源命名补入精简优化计划

- 用户截图显示详情页并列使用“会议回放、复听、电脑播放的声音（独立音轨）、声音片段”；这些标题分别描述合成结果、播放动作、录音来源和分析结果，无法直接回答哪条是麦克风、哪条是电脑声音、哪条是合并会议录音。
- 证据保存到 `doc/audit/2026-08-23-menubar-settings-shortcut/05-recording-source-labels.png`；代码定位到 `RecordingDetailView.swift` 的三个播放器和声音活动检测列表。
- 同一优化计划新增 MSS-07：默认统一为“我的声音（麦克风）”“电脑声音”“会议完整回放”，以“原始音轨/合成回放”文字 Badge 区分；窗口级采集必须明确“当前窗口”。
- “检测到的有声片段”迁入转写分析折叠区，默认文案不再暴露 WAV、CAF、`meetingMix`、Buffer 或峰值；技术事实继续保留在详情和诊断层。
- 本轮只更新计划、索引、日志和截图证据，没有修改播放器 UI、Artifact、录音文件、转写路由或当前安装包。

## Dock 图标圆角补入精简优化计划

- 用户截图确认 Dock 中 Woice 显示为无圆角白色方块；证据保存到 `doc/audit/2026-08-23-menubar-settings-shortcut/04-dock-icon-square.png`。
- 当前安装包已包含 `Assets.car`、`AppIcon.icns`，Info.plist 的 `CFBundleIconName/CFBundleIconFile` 均为 `AppIcon`；问题不是 Bundle 图标元数据缺失。
- 生产和安装包内 `woice-app-icon-1024.png` 均为无 Alpha 的 1024 × 1024 白底 PNG，SHA-256 相同；`WoiceBrandAssets.installApplicationIcon()` 启动时将其赋给 `NSApplication.applicationIconImage`，覆盖系统 Bundle AppIcon，是当前方形显示的直接路径。
- 更新菜单栏/设置/快捷键优化计划，增加 MSS-06 Dock 图标工作包：优先停止生产运行时覆盖，让 Bundle AppIcon 与系统遮罩接管；实测仍异常时再迁移 Icon Composer 分层资源，不手工烘焙任意圆角。
- 本轮只更新计划、索引、日志和截图证据，没有修改品牌母版、运行时代码、打包脚本或当前安装包。

## 菜单栏、设置与快捷键精简优化计划

- 新增 `doc/plan/2026-08-23-menubar-settings-shortcut-optimization.md`，状态为“待实施”；本轮只完成截图审计、根因定位、工作包与验收标准，没有修改 UI、设置持久化、快捷键注册或安装包。
- 菜单栏顶部大块空白根因已定位为 `WoiceMenuBarController` 固定 `336 × 560` Popover，而 SwiftUI 内容只固定宽度；计划改为 fitting size 驱动的动态高度，空闲态不超过 260 pt。
- 菜单栏目标冻结为“开始/结束录音、进入工作台、设置、退出 Woice”四动作；最近结果、导入、文字转音频、首次引导和完整错误诊断迁入工作台，录音/处理/失败只保留紧凑状态。
- 设置页按默认层与高级层减负，常态文案不再暴露 `verbose_json`、CAF、on-device Speech、Provider、Keychain；异常恢复和安全边界仍按需显示。
- 全局录音快捷键计划从三个预设扩展为直接按键录入，保存前用 Carbon 临时注册探测冲突，保存失败恢复旧快捷键；不安装 Event Tap，不请求输入监控权限。
- 审计证据保存在 `doc/audit/2026-08-23-menubar-settings-shortcut/`，包含菜单栏距离、设置首屏和快捷键区域三张已检查截图。

## 启动 Journey 的隐藏 Popover 误报修复

- 稳定 Build B 运行时发现菜单栏 `NSPopover` 会在 System Events AX 窗口列表中留下隐藏的 `AXDialog`（名称为 `Window`），导致验收脚本把它误当成前台工作台并报标题错误；应用实际工作台标题仍为“Woice 工作台”。
- 启动、无障碍和隔离导入验收脚本改为按窗口名称匹配“Woice 工作台”，关闭窗口时只统计该窗口，不再依赖 `front window` 顺序；脚本保留 `zsh -n` 语法门禁。
- 修复后稳定 Build B 复跑通过：`WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window`；`WOICE_RUN_ACCESSIBILITY_JOURNEY=1 WOICE_RUN_KEYBOARD_JOURNEY=1 make acceptance-accessibility-runtime`，结果分别为 `process=1 window=Woice 工作台`、`⌘2 -> 处理任务` 和 AX tree passed。

## 当前完成审计

- 最新 `make verify` 通过：169 项 Swift 测试 / 10 个 Suite、PI 6 项、MCP 2 项、lint、生产构建；专项 `acceptance-workspace-sidebar`、`acceptance-media-import-transcription`（6 项）、`acceptance-permission-continuity`（13 项）和 `acceptance-accessibility-runtime` 契约均通过。
- 稳定 Build B 的启动、关闭、重开、单实例、AX 树和 `⌘2 -> 处理任务` 已由运行时 Journey 通过；真实 TCC 连续性、VoiceOver/高对比/Reduce Motion/字体矩阵、真实录音、系统声音、用户文件、长 Provider 和 CLI Beta 仍明确保留给用户手动路径。
- 本轮没有调用 `tccutil reset`，没有执行真实文件/真实会议/真实第三方 Provider 测试；这些项目不能在当前审计中标记为“已完成”。
- 计划索引和 `doc/INDEX.md` 已从“代码实施中”更新为“代码已完成，真实 Mac 验收待用户”，避免把手动边界混在未完成代码项里。

## 稳定签名 A→B TCC 覆盖安装复跑

- 使用固定 Apple Development 身份 `Apple Development: jianglewis@126.com (23RRB32DPL)`（指纹 `C44F515F60BEDBF8F9E96F4EBECD66D591DCC8B7`，Team ID `C27W5WB6R8`）重新生成并连续覆盖 Build A `2026082301` → Build B `2026082302`。
- 当前证据目录为 `/private/tmp/woice-stable-ab-20260823-182209`，保留 `Woice-installed-A-before-manual-tcc-1.app` 与 `Woice-installed-B-before-manual-tcc-1.app`；当前 `/Applications/Woice.app` 为稳定签名 Build B。
- 已复核 Bundle ID、Team ID、四项 TCC Usage Description 和 `codesign --verify --deep --strict`；未调用 `tccutil reset`，TCC 授权继承与重复弹窗仍由用户按手册手动验收。

## 手动验收运行手册

- 新增 `specs/2026-08-23-manual-acceptance-runbook.md`，覆盖工作台启动/标题/单实例、侧栏密度与按钮反馈、设置分区保存、稳定 A→B TCC、VoiceOver/高对比/Reduce Motion/字体、真实录音/系统声音/文件导入/长文件和 CLI Beta 最后优先级。
- 手册明确本轮 Codex 不执行真实录音、真实文件、真实会议声源和真实第三方 Provider；用户按步骤记录 Build Number、四项权限状态、截图和失败现象。
- `acceptance_stable_upgrade.sh` 增加安全的 `WOICE_STABLE_BACKUP_TAG`，支持同一证据目录内连续 A→B 覆盖而不覆盖历史备份。
- 专项门禁复跑：`acceptance-workspace-sidebar`、`acceptance-media-import-transcription`（6 项）、`acceptance-permission-continuity`（13 项）、`docs-check`、`harness-check`、`lint` 均通过；首次并行沙箱失败已按原文重跑授权上下文。

## 稳定 B 覆盖与窗口标题回归修复

- 稳定 B 首次启动 Journey 暴露两个问题：覆盖脚本调用不存在的 `/usr/bin/proc_pidpath`，旧进程 `86088` 未退出；录音详情的 SwiftUI `navigationTitle` 覆盖了外层窗口标题，导致窗口被读为“录音详情”。
- 覆盖脚本改用 `/bin/ps -p <pid> -o command=` 严格校验 Woice 可执行路径后再停止自身实例；不再误杀其他进程。`WoiceWorkspaceWindow` 固定 AppKit 外层标题为“Woice 工作台”，启动规格和静态门禁同步更新。
- 修复后重新生成并安装稳定签名 A/B：证据目录 `/private/tmp/woice-stable-ab-20260823-182100`，当前 `/Applications/Woice.app` 为 Build B `2026082302`，Apple Development Team ID `C27W5WB6R8`。
- `WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window` 通过：`process=1 window=Woice 工作台`；`WOICE_RUN_ACCESSIBILITY_JOURNEY=1 WOICE_RUN_KEYBOARD_JOURNEY=1 make acceptance-accessibility-runtime` 通过 AX 树与 `⌘2 -> 处理任务`。
- `make test` 通过 169 项 / 10 个 Suite；真实 TCC 授权、真实录音和真实文件导入仍按用户要求保留手动验收，未调用 `tccutil reset`。

## 稳定签名 TCC A/B 覆盖安装

- 新增 `specs/2026-08-23-stable-signing-tcc-ab.md`，冻结同 Bundle ID/Team ID/可执行文件名/权限声明，只改变 Build Number 的 A/B 验收和手动 TCC 路径；明确不调用 `tccutil reset`，本轮按用户要求跳过真实录音与真实文件测试。
- 扩展 `scripts/acceptance_stable_upgrade.sh`：显式要求稳定签名身份，生成 `2026082301`（A）和 `2026082302`（B）Core 包，比较 Bundle ID、Team ID、四项 TCC Usage Description、designated requirement、entitlements 和版本边界；覆盖安装前保留当前 App 备份。
- 本机稳定身份为 Apple Development 指纹 `C44F515F60BEDBF8F9E96F4EBECD66D591DCC8B7`，Team ID `C27W5WB6R8`。A/B 均通过 `codesign --verify --deep --strict` 与稳定身份门禁，证据目录：`/private/tmp/woice-stable-ab-20260823-175813`。
- Build A 已覆盖安装到 `/Applications/Woice.app`；旧 ad hoc 安装包备份为证据目录内 `Woice-installed-A-before-next-step.app`。`WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window` 通过：`process=1 window=Woice 工作台`。
- 当前不宣称 TCC 连续性通过：需先在 A 上由用户逐项确认麦克风、系统音频、Speech、辅助功能，再将证据目录内 Build B 覆盖到 `/Applications/Woice.app`，按规格逐项点击“重新检查”并记录是否重复请求。
- 回归：`make docs-check`、`make harness-check`、`make lint`、`make test` 和 `make verify` 均通过；Swift 为 169 项 / 10 个 Suite，PI 6 项、MCP 2 项。

## 生命周期兜底、真实文件选择证据与外部转写等待态

- `MediaImportSheet` 现在区分 `awaitingAuthorization` 与 `running`：等待用户确认外发时显示“等待你确认外发；原始文件已安全保存在本机”，并禁用重复“转文字”操作；规格同步写入 `specs/2026-08-23-media-import-continuity.md`。
- 发现安装包在无可见窗口时可能只留下菜单栏进程；`WoiceApp` 增加启动时同步显示和 `applicationDidBecomeActive` 兜底，`WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window` 重新通过（process=1，窗口 `Woice 工作台`）。
- 真实用户驱动的 macOS 文件面板已人工走通：选择 `/private/tmp/woice-acceptance-audio.wav` 后导入 Sheet 显示原件文件名、SHA-256 与“转文字”，原件确实落盘；外部 Provider 仅进入等待确认，没有自动发送。由于桌面前台会被其他应用抢占，未把自动点击 Open Panel 的脚本计为通过。
- 本次人工验收产生的唯一测试记录已从正式素材库清理；JSON、SQLite 和两个音频文件备份到 `/private/tmp/woice-acceptance-cleanup-20260823`，可恢复。正式素材库从 12 条恢复为 11 条。
- 回归：`make test` 通过 169 项 Swift 测试 / 10 个 Suite；隔离 `WOICE_RUN_MEDIA_IMPORT_JOURNEY=1 WOICE_MEDIA_IMPORT_SOURCE=/private/tmp/woice-acceptance-audio.wav make acceptance-media-import-desktop` 通过；`make docs-check`、`make harness-check`、`make lint` 通过。
- 最终 `make verify` 通过；`/Applications/Woice.app` 与 `build/Woice.app` 的 Woice 二进制 SHA-256 均为 `ffc920fa6810434b83c0b196ee274c76b026ac9cdf4ab38902230b23aa8caf28`，`codesign --verify --deep --strict` 通过。
- 最终安装包的 `WOICE_RUN_ACCESSIBILITY_JOURNEY=1 WOICE_RUN_KEYBOARD_JOURNEY=1 make acceptance-accessibility-runtime` 通过；AX 树和 `⌘2 -> 处理任务` 路由正常。
- 未完成：真实 Provider 长文件/失败重试、完整 VoiceOver/高对比/字体矩阵、稳定签名下 TCC A/B 覆盖安装和 Developer ID 公证。

## 桌面音视频导入闭环与工作区快捷键复验

- 修复导入文件选择器归属：`.fileImporter` 从父工作区移入 `MediaImportSheet`，选择文件、导入状态和失败反馈保持在同一 Sheet；按钮同时提供 Help/AX 文案。
- 桌面验收使用仅测试参数 `--woice-test-mode`、隔离存储根、源文件和 Fixture 本机转写，不污染 `~/Library/Application Support/Woice`，也不把 macOS 独立 Open Panel 的脚本点击误记为真实用户选择证据。
- 音频 `/private/tmp/woice-acceptance-audio.wav` 和视频 `/private/tmp/woice-acceptance-video.mp4` 的隔离桌面 Journey 均通过：原件 `.source.*`、派生 WAV、SHA-256、Fixture 文本和详情路由均落盘并可见。
- `MediaImportTests` 6 项、权限连续性 13 项、媒体导入转写门禁、`⌘1 -> ⌘2` AX Journey 和启动/关闭/重开 Journey 均通过。
- 最终 `make verify` 通过 169 项 Swift 测试 / 10 个 Suite、6 项 PI Node、2 项 MCP Node、docs-check、harness-check、lint 和生产构建；`make install` 已更新 `/Applications/Woice.app`，安装二进制 SHA-256 为 `7c152d098d0554801553bb921da223db8203fbf98ef42d3a85c6a263a6791b7c`，`codesign --verify --deep --strict` 通过。
- 发行脚本新增显式 `WOICE_CODESIGN_IDENTITY`；默认仍使用 ad hoc。用本机 Apple Development 身份生成的 `build/Woice-Development.app` 通过 `WOICE_RUN_STABLE_UPGRADE=1 WOICE_STABLE_APP_PATH=build/Woice-Development.app make acceptance-stable-upgrade`，Team ID 为 `C27W5WB6R8`；尚未安装该包或宣称 TCC A/B 连续性完成。
- 仍未完成：真实用户驱动文件选择、真实 Provider 长文件/失败重试、VoiceOver/高对比/字体矩阵、稳定签名下的 TCC 覆盖安装；当前包仍是 ad hoc。

## 工作区键盘入口补齐

- 工作区侧栏为素材库、处理任务、文字转音频、设置增加稳定的 `⌘1`–`⌘4` 快捷键，并把快捷键写入 VoiceOver/辅助功能提示。
- AppKit 主菜单新增“工作区”命令组，快捷键与侧栏路由共享 `WorkspaceRouter`；避免纯 SwiftUI 侧栏快捷键在 AppKit 组合根中丢失事件。
- `WOICE_RUN_KEYBOARD_JOURNEY=1 WOICE_RUN_ACCESSIBILITY_JOURNEY=1 make acceptance-accessibility-runtime` 已通过先用 `⌘1` 回到素材库、再用 `⌘2` 切换“处理任务”的真实 Journey；完整 Tab 顺序、VoiceOver、高对比、Reduce Motion 和字体缩放仍需在用户系统设置下实测。

## 工作台重开、导入失败恢复与 AX 运行时门禁

- 窗口生命周期：改为纯 AppKit NSApplicationDelegate 组合根；主菜单显式提供 Woice 退出和“窗口 > 关闭窗口（⌘W）”，统一路由到 WoiceWorkspaceWindowController。窗口显示后在 run loop 再次激活，避免 Finder/Dock 双击只启动进程却不前置工作台。
- 真实 Journey：WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window 通过启动 -> ⌘W 关闭 -> open -a 重开，结果为 process=1 window=Woice 工作台；System Events 读取 frontmost=true、窗口数 1/0/1。
- 媒体导入：新增 2 项 AppState 集成测试，覆盖成功导入的原件/派生音频/ProcessingTask 持久化、外部源文件变更后原件仍不变，以及损坏视频失败时无记录/无残留文件；MediaImportTests 共 6 项通过。
- 导入标题：受控目录文件名改为 UUID.source.原始文件名，Record 标题还原用户文件名，不暴露 UUID；新增 acceptance-media-import-desktop，显式提供文件路径后可通过 System Events 完成文件选择、导入准备和“稍后处理”回路。
- 无障碍：新增 make acceptance-accessibility-runtime；WOICE_RUN_ACCESSIBILITY_JOURNEY=1 读取已安装窗口的 AX 树，验证“导入音频或视频”“打开处理任务”提示和搜索框 text field 均可见；键盘 Journey 额外验证 `⌘1 -> ⌘2` 路由。VoiceOver、高对比、Reduce Motion 和字体放大仍需用户系统设置下完成。
- 当前包仍为 ad hoc；稳定签名、TCC 连续性、Finder 实际选择音/视频和完整键盘/VoiceOver 矩阵仍不宣称完成。最终安装二进制 SHA-256 为 7a2059a54ed7d07e1941ae2083e923968803b050f782c6e197a574aef2e0b452。

## 双击启动与工作台窗口连续性

- 根因：应用使用 LSUIElement=true 且只有菜单栏场景，Finder 双击后进程会运行，但没有可见工作台窗口，用户误以为未启动。
- 修复：关闭 LSUIElement，改用 regular App + AppKit NSStatusItem 菜单栏控制器，并由 WoiceWorkspaceWindowController 承载统一工作台；菜单栏“历史/设置/导入”复用同一窗口控制器。移除 MenuBarExtra，避免其将激活策略压成 prohibited 导致窗口对象可见但无法置前。
- 验证：重新安装后通过 `WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window` 启动、⌘W 关闭、open -a 重开，进程数为 1，System Events 读取到“Woice 工作台”；新增 acceptance-launch-window 代码门禁和可选真实 Journey。
- 回归：AppKit 状态栏入口、退出/导入/设置路由共享同一 AppState 与 WorkspaceRouter；`make lint` 无警告，最终 `make verify` 为 169 项 Swift 测试 / 10 个 Suite、6 项 PI Node、2 项 MCP Node、生产构建。

## WPC 代码收口与当前安装

- 全量 make verify 通过：169 项 Swift 测试 / 10 个 Suite、6 项 PI Node、2 项 MCP Node、lint、生产构建。
- acceptance-workspace-sidebar、acceptance-media-import-transcription、acceptance-permission-continuity 通过；acceptance-stable-upgrade 按预期跳过，等待稳定签名 A/B 包。
- 本机签名证据：`security find-identity -v -p codesigning` 返回 `0 valid identities found`；安装包 `codesign -dvv` 为 `Signature=adhoc`、`TeamIdentifier=not set`，因此不把当前包用于 TCC 权限继承结论。
- 稳定签名门禁显式运行已 fail-closed：`WOICE_RUN_STABLE_UPGRADE=1 make acceptance-stable-upgrade` 拒绝当前 adhoc 包；默认运行保持跳过，等待 A/B 稳定签名产物。
- 最后一次 make install 已安装 /Applications/Woice.app；当前 Woice 二进制 SHA-256 为 7a2059a54ed7d07e1941ae2083e923968803b050f782c6e197a574aef2e0b452。

## WPC-04 长文件分段补齐

- 新增 AudioChunkingService：外部 ASR 音频超过 20 MiB 安全阈值时按帧边界生成临时 WAV 分段，逐段上传并把返回时间戳偏移回原始时间轴；完成或失败后清理工作目录，原件不变。
- MediaImportTests 增加分段顺序、无重叠、可读性和损坏媒体 fail-closed 验证；真实 Provider 限制与桌面 Journey 仍需用户在目标环境复验。
- 本次 WPC-04 验证总数为 4 项；下方早期记录中的“2 项”是导入分段补齐前的快照。

## WPC-00～06 工作区、导入与权限代码实施

- WPC-00 基线完成：确认当前工作台为三列导航、没有音视频导入入口，Recording 原先没有来源和原件元数据。
- WPC-01～03：工作台改为单一 280～360 pt 左侧栏，合并工作区入口、素材搜索/状态筛选、设置分区和处理上下文；素材空状态提供“开始录音”“导入音视频…”。
- WPC-04：新增音视频导入 Sheet、文件选择器、原件受控复制、来源类型、原件 SHA-256/字节数、视频音轨提取和 16 kHz 单声道派生 WAV；导入不自动外发，支持“转文字/稍后处理/打开原件”。
- WPC-05～06：保留复制与自动粘贴权限拆分、系统声音重新授权诊断，并新增权限连续性专项门禁；稳定签名覆盖安装和真实 TCC 仍由用户完成。
- 验证：`MediaImportTests` 2 项、`acceptance-workspace-sidebar`、`acceptance-media-import-transcription`、`acceptance-permission-continuity` 代码门禁通过；`acceptance-stable-upgrade` 默认明确跳过未提供稳定签名包的真实验收。
- 规格：[2026-08-23-media-import-continuity.md](../specs/2026-08-23-media-import-continuity.md)；附件计划已同步 WPC 工作包状态，CLI Beta 未完成项仍保持最后优先级。

## CLI Beta 命名与未完成项后置

- 前端可见的 Codex CLI、Claude Code CLI、受控 CLI Runner、派发入口和任务列表统一标记为 `Beta`；内部 connector ID、Manifest 协议和任务数据不改名。
- `doc/plan/2026-08-23-workspace-sidebar-and-permission-continuity.md` 新增第 11 节“最后优先级：CLI Beta 未完成项（暂不实施）”，集中记录 Codex 交互批准、真实用户素材入站、可配置权限/二次确认、循环攻击矩阵和稳定发行验收。
- 本轮只改用户可见文案和计划排序，不启动 WPC-00～08，不改变录音、转写、系统权限或 Agent 运行边界。
- 验证：`make verify` 通过（163 项 Swift 测试 / 9 个 Suite、6 项 PI Node、2 项 MCP Node、lint、生产构建）；`make docs-check` 与 `make harness-check` 通过。
- 安装：`make install` 已更新 `/Applications/Woice.app`，安装二进制 SHA-256 为 `452fbe98043d8dec8cd59985a7fb66642c94dbd452eadafe60461cc93948d352`，并已重新启动应用。

## 音视频导入连续转写整合进工作区计划

- 更新统一工作台规格与“工作区侧栏与权限连续性优化计划”：范围增加本地音视频导入、来源标识、原件不可变、导入后原位“转文字”和成功自动选中新素材。
- 工作台入口冻结为素材库/素材详情工具栏的 `[导入音视频…] [开始录音]`；素材库空状态提供双入口，设置、处理任务和文字转音频页不显示导入入口。
- 菜单栏增加次级“导入音视频…”并复用同一文件选择和窗口级 Sheet；Sheet 连续承载导入、准备、待转写、转写中、失败与完成，不要求用户回列表寻找文件。
- 新增 WPC-04 音视频导入与连续转写工作包，原 WPC-04～07 顺延为 WPC-05～08；计划继续保持“待实施”，本轮未启动 UI、媒体处理、签名或安装改动。
- 后续处理边界不变：导入内容复用现有 ASR、ProcessingTask、Lease、配置快照、Transcript Artifact、搜索、导出和 Agent 路径，不创建第二套体系。

## 工作区侧栏与权限连续性优化计划建档

- 新建 `doc/plan/2026-08-23-workspace-sidebar-and-permission-continuity.md`，状态为“待实施”，只有用户明确要求“启动工作区与权限优化计划”后才进入 WPC-00。
- UI 范围冻结为单一左侧栏、当前工作区列表、素材行密度、空状态、工具栏可理解性和无障碍；不新增 AI 对话或 AI 处理工作区。
- Agent 边界保持不变：继续使用 M2-09 已有 `发送给 Agent…`、受控 CLI、任务状态和结果 Artifact，不修改协议或排期。
- 权限范围冻结为复制/自动粘贴拆分、逐项权限 UX、稳定代码身份和真实覆盖安装矩阵；证书创建、Developer ID、公证和 App Store 签名仍由既有发行计划负责。
- 本轮只建档计划与索引，未修改业务代码、当前路线图、签名、TCC 权限或安装包。

## M2-09 出站派发、结果 Artifact 与审计切片（状态更新）

- 实现：录音详情新增三步“发送给 Agent”流程；显式发现 Codex CLI/Claude CLI，固定只读目录、版本探针、超时、取消、输出上限和结果文件占位符。
- 结果：Agent 返回内容写入 agent-results/，创建带录音/输入 Artifact/Connector/版本/SHA-256 的不可变结果 Artifact；详情页可预览、复制和在 Finder 中显示，不执行返回内容。
- 审计：新增 SQLite schema v4 和 AgentAuditEvent；出站请求/开始/完成/失败/取消以及 PI 入站读取均只写元数据，不写原文、音频、Prompt、密钥或结果正文。
- 验证：AgentDispatchTests fixture 通过，确认结果内容、审计事件和原始音频不变；SQLite 审计持久化测试与只读权限拒绝测试已加入；最新 `make verify` 通过（162 项 Swift 测试 / 9 个 Suite、6 项 PI Node、2 项 MCP Node、lint、生产构建）。
- 门禁：新增 make acceptance-agent-outbound / acceptance-agent-inbound。前者默认只运行本机 fixture，真实 Codex/Claude 和外部 Agent Smoke 仍需显式环境变量与用户授权，不把 fixture 结果宣称为真实 CLI 完成。
- 真实出站：`WOICE_RUN_REAL_AGENT=1 make acceptance-agent-outbound` 通过；Codex CLI 0.147.0 与 Claude Code CLI 2.1.233 均完成合成素材派发并回收结果 Artifact，原始音频 SHA-256 保持不变。
- 真实本机入站：`WOICE_RUN_REAL_INBOUND=1 make acceptance-agent-inbound` 通过；真实 MCP/RPC 桥接完成 5 个只读工具、11 条录音列表和分页读取 Smoke，并写入元数据审计。
- 安装包复验：重新启动 `/Applications/Woice.app` 后再次运行同一入站 Smoke，仍以 5 个工具、11 条录音和 29 字节分页结果通过，确认不是旧进程。
- 外部入站：新增 `scripts/acceptance_agent_external_inbound.mjs` 与 `make acceptance-agent-external-inbound`；每个 Agent 使用独立临时合成 Woice Socket，避免跨 Agent 统计造成假阳性。Claude Code 通过真实 MCP Bridge 调用了 4 个只读方法；Codex 非交互模式对 MCP 工具请求用户批准，未使用自动绕过，故仍保留待交互验收。
- 权限/UX：派发确认页新增三档权限语义、显式“创建任务”摘要和二次勾选；只读权限拒绝出站任务并有契约测试，控制正在录音保持关闭。
- 未完成：外部 Agent 实际读取用户素材（需用户主动选择非敏感素材并授权）、连接器三级权限 UI/二次确认与循环攻击矩阵、正式签名/公证和系统音频 TCC 真实复验。当前不把本机 MCP Smoke 宣称为两个外部 Agent 已完成。
- 发行：二次确认 UI 改动后的源码已重新安装到 `/Applications/Woice.app`，安装二进制 SHA-256 为 `6b47251635494035699d2f65f188b8e5c54d7ad371daa8f787f16e770e842e4d`；Core/Offline DMG 已重新构建并通过 `codesign --verify --deep --strict` 与 `hdiutil verify`。当前仍是 ad hoc 签名，不等同 Developer ID/公证。

## Mac App Store 上架计划建档

- 计划：新增 `doc/plan/2026-08-23-mac-app-store-launch.md`，状态为“待实施”，仅记录上架准备、必须清单、Store 能力矩阵、工作包、门禁和审核路径。
- 裁决：Mac App Store 只规划一个开箱可用的 Store Edition；官网 Core/Offline 双版本继续由 M1-07/M2-08i 管理，Store 计划不替代 Developer ID/公证和双发行验收。
- 边界：未启动任何开发、签名、账号、上传或审核操作；只有用户明确要求“启动上架计划”后才进入 MAS-00。

## M2-09f Agent 状态 UI 与最新回归

- UI：设置新增只读“Agent 与连接”分区，展示素材只读查询、Context Package、受控 Runner 三项已验证基础；未配置真实连接时明确说明，不伪造“已连接”。
- 任务：处理工作区按录音处理 / Agent 任务分组，显示 Connector、状态、更新时间和错误；`interrupted` 明确“未自动重放”。
- 文档：新增 `specs/2026-08-23-agent-job-ui-status.md`，将 M2-09f 的状态 UI 与真实派发流程、结果 Artifact 分开。
- 回归：新增 UI 后 `make docs-check`、`make harness-check`、`make lint`、完整 `swift test --no-parallel`（157 项 Swift / 7 个 Suite）和 PI/MCP Node 测试均通过；最新生产包已生成并安装。
- 发行：包含该 UI 的 Core/Offline DMG 均重新签名，`codesign --verify --deep --strict` 和 `hdiutil verify` 均通过。
- 仍待：录音详情三步“发送给…”、Codex/第二个 CLI 真实适配、结果 Artifact 回收和外部 Agent Smoke；核心录音、转写、模型与素材库不依赖本分区。

## M2-09a/b/c + 发行门禁最新复核

- 门禁：最新 `make verify` 通过，包含 docs/harness、6 项 PI Node 测试、2 项 MCP Node 测试、lint、157 项 Swift 测试（7 个 Suite，0 失败）和生产构建。
- 安装：`make install` 已将与当前源码同 SHA-256 的二进制安装到 `/Applications/Woice.app`；安装包二进制与 `build/Woice.app` 一致。
- 发行：`make verify-core` 的 Core DMG 代码签名与 `hdiutil verify` 通过；`make verify-offline WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice"` 的 Offline DMG 代码签名与 `hdiutil verify` 通过。
- M2-09：Context Package、受控 CLI Runner、SQLite durable Agent Job 和只读 MCP Bridge 的本机契约已验证；真实 Codex/第二个 CLI、结果 Artifact、用户派发 UI、真实 Agent Smoke 仍未完成。
- 外部边界：当前 App 仍为 ad hoc 签名；Developer ID/公证、生产 Catalog host/key、当前安装包 TCC 真实系统声音复验和长录音/睡眠/崩溃/设备变化矩阵仍需真实 Mac/发行环境。

## M2-09e 只读素材搜索与分页读取

- RPC：新增 `woice.search_materials` 和 `woice.read_material_page`；搜索沿用素材库 AND 语义，分页按 Unicode 字符边界并限制单页 UTF-8 不超过 64 KiB。
- 安全：两个方法只读，不创建 Job、不启动录音、不读取 Keychain、不发送网络；非法分页、未知字段和缺失原文 fail-closed。
- PI：新增 `woice_search_materials`、`woice_read_material_page` 两个薄适配工具，仍只调用 Woice 版本化 Unix Socket。
- 验证：9 项 Swift PI/RPC 测试和 6 项 Node PI 测试通过；真实 PI 安装和外部 Agent Smoke 仍待 M2-09 正式启动。

## M2-09a/b/c Context Package、受控 Runner 与 durable Job

- 契约：新增 `ContextPackage`、`AgentCLIAdapterManifest`、`AgentDispatchJob`、有限状态/能力/传输枚举和稳定错误校验；拒绝重复 Artifact、路径穿越、Shell 元字符、未知占位符、任意环境变量和 hop 超限。
- Builder：`ContextPackageBuilder` 只复制显式选择的音频，生成 `context.json`、`transcript.md` 和只读临时文件；文本、音频有上限，原始素材不修改，包 hash 对相同输入稳定。
- Runner：`ControlledCLIRunner` 使用固定 executable/参数、临时诊断目录、版本探针、环境白名单、超时、取消、进程组回收和输出上限；未授权/非零退出/超限均 fail-closed。
- Durable：SQLite schema v3 新增 `agent_dispatch_jobs`，按幂等键更新；重启时 `launching/running/collecting/awaitingAgentApproval` 恢复为 `interrupted`，不自动重启或外发。
- 验证：6 项 Agent 契约/Builder 测试、3 项 Runner 失败矩阵测试和 1 项 SQLite Agent Job 迁移/恢复测试通过；真实 Codex/PI/其他 CLI、结果 Artifact 和用户派发 UI 仍后置。

## M2-09e 完整门禁复跑与安装

- 完整 `make verify`：docs-check、harness-check、6 项 Node PI 测试、lint、146 项 Swift 测试（5 个 Suite，0 失败）和生产构建均通过；PI 测试需要允许临时 Unix Socket，SwiftPM 需要本机 ModuleCache。
- 安装：通过 `make install` 更新 `/Applications/Woice.app`；`make verify-core` 与 `WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make verify-offline` 均通过，Core/Offline DMG `hdiutil verify` 均为 VALID。
- 边界：这证明本机可验证交付与 M2-09e 只读契约完整，不等于真实 PI 安装、两个外部 Agent Smoke、正式签名/公证或生产 Catalog 已完成。

## 真实桌面录音 Journey 与 TCC 状态修正

- 真实 UI Journey：通过已安装 `/Applications/Woice.app` 的工作台工具栏开始录音，界面先显示“正在请求麦克风权限”，录音中持续显示时长，约 58 秒后点击停止；新 WAV 落盘为 11.2 MB，列表状态进入“素材已就绪”，Large-v3 本机转写完成并展示原文、时间戳和模型快照。
- 系统声音边界：本次录音未生成系统音频独立轨，原因是当前安装包的 ScreenCaptureKit/TCC 有效授权仍未通过；不能把系统设置里显示的 Woice 开关直接当作当前运行实例可采集的证据。
- 诊断事实：系统设置“录屏与系统录音”列表中 Woice 当前显示开启，但切换该开关要求用户触控 ID/密码；助手未代填认证。该现象符合 ad hoc 重装后旧代码签名哈希与当前安装实例不一致的风险。
- 代码修正：`SystemAudioCapabilityService` 增加“需要重新授权当前安装包”状态；请求权限后的轮询仍失败时，设置页明确提示关闭旧项、重新打开当前 Woice 并由用户完成系统认证，不再重复显示含糊的“需要屏幕录制权限”。新增状态契约测试通过。

## 完整 Swift 门禁与发行产物复核

- 测试基础修正：模型包自动路由测试改为显式持久化用户选择，避免把任意测试模型误当作默认模型；设置候选保存测试使用测试专用的真实 Security.framework 临时钥匙串，生产 `KeychainStore` 未改变。
- 完整门禁：最新 `make verify` 通过，包含 `docs-check`、`harness-check`、5 项 Connector 测试、生产构建和 145 项 Swift 测试（5 个 Suite，0 失败）。当前用户 login Keychain 仍返回“用户名或密码不正确”，但已不再污染自动化测试的隔离钥匙串。
- 安装与发行：`make install` 已将最新 `Woice.app` 安装到 `/Applications/Woice.app`；授权状态修正版之后重新运行的 `make verify-core` 与 `WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make verify-offline` 均通过，Core/Offline DMG 的 `hdiutil verify` 均为 VALID。
- 仍未完成：Developer ID/公证、生产 Catalog host/key、干净账户覆盖安装、真实全桌面/多窗口/长录音与睡眠/崩溃/设备变化矩阵、真实会议人工语料准确率和 M2-09 Agent 交接。

## 系统音频验收声源修正

- 根因：窗口级 ScreenCaptureKit 回退收到 buffer 并不等于捕获到可听声音；原验收脚本用无窗口的 `afplay` 播放，最新结果峰值为 `0.0`，未生成 meetingMix。
- 修正：`specs/2026-08-23-real-meeting-acceptance.md` 和 `scripts/acceptance_meeting.sh` 现在使用临时合成的 20 秒音频，由可见 QuickTime Player 循环播放；QuickTime 无法启动或当前会话锁定时响亮失败。
- 当前验证：`zsh -n scripts/acceptance_meeting.sh`、`make docs-check`、`make harness-check`、`make lint` 通过；解锁桌面后 `make acceptance-meeting` 已通过（QuickTime 可听系统音频、CAF 与 meetingMix）。

## 五类 300 秒模型基准与默认模型冻结

- 工具：新增 `specs/2026-08-23-model-benchmark-fixture-generator.md` 和 `scripts/create_model_benchmark_fixtures.sh`；`make model-benchmark-fixture` 只在显式目录生成中文、英文、混合、静音、噪声五类 16 kHz 单声道 PCM WAV，拒绝覆盖已有文件，不读取用户录音。
- 证据：使用修正版 Fixture 目录 `/private/tmp/woice-model-benchmark-300-v2`，每类 300 秒；`make model-benchmark-strict` 对固定 revision 的 Tiny 和 Large-v3 均通过，五类覆盖完整、无失败/空输出、RTF ≤ 1、内存 ≤ 4 GiB。结果保存在 `build/model-benchmark-tiny-300-v2.json` 和 `build/model-benchmark-large-300-v2.json`，汇总见 `doc/benchmarks/2026-08-23-whisperkit-300s-matrix.md`。
- 结果：Tiny 最差 RTF 0.041、峰值约 934.5 MiB；Large-v3 最差 RTF 0.267、峰值约 1,207.4 MiB。两者均满足性能门禁；本次是可复现系统语音性能样本，不替代真实会议人工语料准确率。
- 路由：新增 `specs/2026-08-23-default-model-freeze.md`；未显式选择时优先已验证 Large-v3，用户显式选择优先，Large 缺失/损坏时回退 Tiny，再回退 macOS Speech。新增启动路由测试通过，设置页文案同步更新。
- 安装与发行：最终路由代码已通过 `make install` 安装到 `/Applications/Woice.app`；`make verify-core` 与 `make verify-offline WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice"` 再次通过，两个 DMG 的 `hdiutil verify` 均为 VALID。
- 真实本机闭环：默认路由代码下 `make acceptance-whisperkit` 通过，固定模型安装/读取、真实麦克风录音和本机 WhisperKit 转写均有证据。
- 最新系统音频复跑：修正为可见 QuickTime 播放源后，`make acceptance-meeting` 通过（15.432 秒）；窗口级采集生成可听系统音频和 meetingMix。此前 `afplay` 的 723 个静音 buffer 仅作为失败诊断，不再作为当前结论。
- 边界：正式 Developer ID/公证、生产 Catalog host/key、真实会议/全桌面 UI Journey、长录音矩阵和完整 login Keychain 可写状态仍未完成。

## 识别语言与转写 Artifact 版本链

- 规格：新增 `specs/2026-08-23-language-picker-ux.md` 和 `specs/2026-08-23-transcript-artifact-lineage.md`，分别冻结语言选择/自动检测语义和原文版本不可覆盖边界。
- 设置实现：识别语言改为原生 Picker；新设置默认保存空值并显示“自动检测（推荐）”，旧 `zh`/`en`/区域代码映射为可读名称，未知代码原样保留。macOS Speech/实时预览在自动检测时使用系统首选语言，WhisperKit/外部接口继续保留空值自动检测。
- 素材实现：新增 `TranscriptArtifact` 版本链；首次转写、模型切换重转写和会议来源转写均追加版本，保存 `parentRecordingID`、`supersedesID`、Provider/模型/版本/数据位置/配置摘要，当前原文只是可切换投影。
- UI/导出：录音详情在多个版本时显示模型快照和“查看”切换；时间戳 JSON 增加就绪状态、active Artifact 和完整版本链，不写入 Keychain/API Key。
- 验证：语言、旧数据兼容、首次本机转写、Large 重转写保留旧原文、历史版本切换和开放导出测试共 7 项通过；`make docs-check`、`make harness-check`、`make lint` 通过。

## 升级计划收尾复跑（历史记录，最新结论见顶部）

- 代码门禁：最新代码通过 `make acceptance-core`（真实麦克风、本机/自定义 ASR、播放元数据）、`make acceptance-meeting-transcription`（标准单请求/来源分离双请求契约）和修正声源后的 `make acceptance-meeting`（15.432 秒，可听系统音频 + meetingMix）。
- 文档与质量：`make docs-check`、`make harness-check`、`make lint` 均通过。
- 实际包：`make install` 已将本轮源码安装到 `/Applications/Woice.app`；`make verify-core` 和 `make verify-offline WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice"` 均通过，Core/Offline DMG 的 `hdiutil verify` 均为 VALID。
- 完整回归：本段记录当时的 login Keychain 阻塞；随后已改为测试专用真实临时钥匙串并重跑，最新 `make verify` 已通过 144 项 Swift 测试。生产环境 login Keychain 的密码/解锁状态仍需用户在本机修复后单独验证。
- 计划状态：本机可验证的录音、转写、五类模型性能矩阵、默认模型路由、模型库存/下载、会议窗口级采集、设置分区独立保存、Keychain 延迟读取和 Core/Offline 打包切片已完成；真实桌面矩阵、真实会议/人工语料准确率、生产 Catalog、公证/签名和 M2-09 Agent 交接仍是待开发/待验收清单。

## 系统音频窗口级回退

- 规格：新增 `specs/2026-08-23-system-audio-window-fallback.md`，冻结 display > active non-Woice window > visible non-Woice window 的确定性优先级；无目标时继续 fail-closed。
- 实现：ScreenCaptureKit 有可共享显示器时保持全桌面采集；无显示器但存在可捕获窗口时使用独立窗口 filter，能力页显示“仅窗口级系统音频可用”。
- 可见性：`RecordingRecord` 新增可选 `systemAudioCaptureTarget`，录音中、详情页和旧数据解码均明确区分全桌面与活动窗口声音。
- 先前 smoke：一次 `make acceptance-meeting` 曾验证过系统声音 buffer、CAF、meetingMix、16 kHz 单声道和标准模式单请求。
- 最新真实验收：更换为可见 QuickTime 播放源后，`make acceptance-meeting` 通过（15.432 秒）；窗口级目标生成可听系统音频、CAF 和 meetingMix。当前环境仍不是全桌面覆盖证明。
- 回归边界：早期回归曾因当前 login Keychain 不可写失败；后续测试专用真实临时钥匙串已消除该自动化环境耦合，最新 `make verify` 已全绿。真实 login Keychain 仍属于本机权限状态，不代表发布环境已完成。

## 升级计划门禁复跑与安装状态

- 本轮专项门禁通过：`make acceptance-core`、`make acceptance-meeting`、`make acceptance-meeting-transcription`、`make acceptance-interruption`、`make acceptance-settings`、`make acceptance-material`、`make acceptance-recovery`、`make acceptance-catalog`、`make acceptance-local-provider`。
- Offline 发行校验通过：`WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make acceptance-offline-model`；bundled inventory、Offline DMG、代码签名和 `hdiutil verify` 均通过。
- Core 发行校验通过：`make verify-core`；Core DMG 的代码签名和 `hdiutil verify` 均通过。
- 文档、Harness、Swift lint 和 Connector 门禁均通过：`make docs-check`、`make harness-check`、`make lint`、`make connectors-check`（5 项测试）。
- 安装状态：`make install` 已将当前源码构建包安装到 `/Applications/Woice.app`。
- 未完成项：完整 `make test` 本轮执行 144 项时唯一失败为当前 login Keychain 返回“用户名或密码不正确”；在钥匙串恢复可写前，不把 `make verify` 记为通过。全桌面/多窗口/真实会议应用、长录音/睡眠/崩溃/设备移除/磁盘不足矩阵、真实会议/人工语料准确率、生产 Catalog host/key、Developer ID/公证和真实 UI Journey 仍待验收。

## Keychain 状态诊断

- 规格：新增 `specs/2026-08-23-keychain-state-diagnostics.md`，冻结 Security 状态到可执行用户文案的映射；不自动解锁、不改变 account、不把状态写入日志或配置。
- 实现：`KeychainStore` 现在区分钥匙串未解锁、访问拒绝、缺少签名权限、用户取消和未知状态；`AppState` 原有失败回滚与分区独立保存语义不变。
- 验证：新增状态映射纯契约测试；真实 login Keychain 锁定/授权状态仍需用户在本机钥匙串可控时复验。
- 历史回归：`make acceptance-settings`（当前 5 项）、一次 `make acceptance-meeting`、`make verify-core` 和 `WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make acceptance-offline-model` 曾在诊断改动后通过；最新系统音频复跑已使用可见 QuickTime 并通过；最新包已重新安装到 `/Applications/Woice.app`。
- UI 回归：设置页底部说明现在按“录音与输入 / 模型与转写 / 文件与隐私”分区显示，不再让普通设置保存看起来像会触碰 Keychain；UI 改动后的 `make verify-core` 和 `make verify-offline WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice"` 均通过。

## Keychain 延迟读取

- 规格：新增 `specs/2026-08-23-keychain-lazy-read.md`，冻结启动、录音/文件设置和本机 Provider 不主动读取 API Key 的边界。
- 实现：AppState 初始化不再读取两个 Keychain account；进入“模型与转写”分区或创建外部 ASR/Markdown 请求前按需加载；已有运行时密钥不被延迟读取覆盖。
- 验证：`make acceptance-settings` 现覆盖 5 项测试，包括启动零读取、一次性延迟加载、分区独立保存、密钥差异写入和 Keychain 状态诊断。

## 录音磁盘空间预检

- 规格：新增 `specs/2026-08-23-recording-storage-preflight.md`，冻结 256 MiB 启动保护线、容量不可读兼容策略和不伪造成功状态的边界。
- 实现：`RecordingService` 在请求麦克风权限、创建 `AudioWriter` 前读取保存卷 `volumeAvailableCapacityForImportantUsageKey`；低于安全线直接返回稳定 `WoiceError`，不创建 Journal、音频引擎或处理任务。
- 语义：这是启动保护线，不是任意长会的容量保证；容量不可读时保留实际写入错误和部分素材恢复路径。
- 验证：新增 2 项容量策略测试通过；写入异常但 WAV 仍可读取时保留部分录音并显式标记错误，新增部分录音保留策略测试；真实磁盘不足、长录音和崩溃矩阵仍需桌面验收。
- 末次更正：随后增加系统音频无采集目标文案契约与部分录音保留策略，最终 `make verify` 为 137 项 Swift 测试、5 项 Connector 测试；最新安装包和 Core/Offline DMG 已再次复核通过。
- 发布回归：最终 `make test` 通过 137 项 Swift 测试；最新包已安装到 `/Applications/Woice.app`，`make verify-core` 与 `WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make verify-offline` 均通过，两个 DMG 的 `hdiutil verify` 均为 VALID。

## 设置分区独立保存

- 规格：新增 `specs/2026-08-23-settings-section-save.md`，冻结录音、模型与转写、文件与隐私的字段边界和独立提交语义。
- 实现：`SettingsView` 改为“保存本页/还原本页”，`AppState.saveSettings` 支持分区范围；保存时只把活动分区合并到当前已提交设置，其他分区草稿不参与校验或提交。
- 安全：录音/文件分区保存不会读取、写入或删除 Keychain；模型分区仍按账号仅处理发生变化的 API Key。
- 验证：新增“其他分区非法草稿不阻塞”“文件分区独立保存”两项测试，连同 Keychain 访问范围回归共 3 项通过；新增 `make acceptance-settings` 作为专项门禁。
- 末次复跑：沙箱内直接执行该命令曾因 SwiftPM 用户级 ModuleCache `Operation not permitted` 退出码 2；按本机权限重跑后 3 项专项测试通过，记录为环境限制而非代码失败。

## 系统音频权限诊断与真实采集边界

- 修正：`SystemAudioCaptureService` 继续以运行时 `SCShareableContent` 为主证据；仅在读取失败或显示器为空后辅助检查 `CGPreflightScreenCaptureAccess()`，把屏幕录制未授权映射为可执行的系统设置提示，把已授权但无可共享目标保留为独立运行时失败。
- 测试：新增系统音频权限/无采集目标错误文案契约；无目标错误现在明确提示解锁桌面、退出远程桌面后重试。
- 门禁：设置分区保存后最新 `make verify` 通过 137 项 Swift 测试、5 项 Connector 测试、lint、文档门禁和生产打包。
- 发行回归：`make install` 已安装最新包到 `/Applications/Woice.app`；最新 Core/Offline DMG 均通过 `hdiutil verify`，两个发行产物门禁通过。
- 真实复跑：最新安装包再次运行 `make acceptance-meeting`，以退出码 2 失败；原文仍为“系统音频未开始：找不到可用的系统音频采集目标。；麦克风仍会继续录音。”，未生成系统 CAF/meetingMix。当前环境边界已被最新包复现，不将其伪装成通过。
- 安装：`make install` 已将最新包安装到 `/Applications/Woice.app`；`make verify-core` 与 `WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make verify-offline` 均通过，两个 DMG 的 `hdiutil verify` 均为 VALID。
- 真实边界：本轮在当前环境运行 `make acceptance-meeting` 失败，原文为“系统音频未开始：找不到可用的系统音频采集目标。；麦克风仍会继续录音。”，随后没有生成 CAF/meetingMix。判定为当前测试环境没有可用 ScreenCaptureKit 采集目标，不把它记为系统声音已验收；需要在有显示器、有效 TCC 授权和可播放系统音频的桌面环境复验。

## 后台分段转写耐久化

- 规格：新增 `specs/2026-08-23-background-transcription-durability.md`，冻结“VAD 片段完成即持久化、停止时合并、成功提交后清理”的边界；sidecar 只存片段索引、规范化文本、失败索引和 Provider/模型快照，不包含 API Key 或原始音频。
- 实现：`WorkspaceStore` 增加原子保存/读取/清理与 fail-closed 校验；`AppState` 在本机后台片段成功或失败后即时更新 sidecar，停止时把已完成片段写入 Recording，异常退出恢复部分原文并保留可重试的 `interrupted` 任务。
- 语义：原始 WAV 仍是不可变真相源；有部分原文但存在未完成/失败片段时投影为“部分原文已就绪”，不误报为完整就绪。
- 验证：`swift test --filter RecordingSessionRecoveryTests` 4 项通过，覆盖 sidecar 恢复、损坏 fail-closed、清理和原始音频 SHA-256 不变；真实长会、睡眠/崩溃矩阵仍待桌面验收。
- 回归：最新代码通过 `make acceptance-interruption`（1.214 秒，真实麦克风配置变化安全停止）；系统声音真实验收见本日志顶部的环境边界记录。

## ProcessingTask 配置快照

- 规格：新增 `specs/2026-08-23-processing-configuration-snapshot.md`，冻结 `sha256-v1` 摘要字段与 API Key/URL 凭据排除规则；历史任务缺失摘要时兼容读取。
- 实现：本机、外部 ASR、分段 ASR、LLM 和重试任务在创建/授权/运行时写入同一配置摘要；后台 sidecar 也保留该摘要，异常恢复后的任务仍可追溯实际模型。
- 验证：本机模型闭环与外部 ASR 失败重试测试通过；重试时替换 API Key 不改变配置摘要，任务 JSON 只保存摘要而不保存密钥。随后完整 `make verify` 通过 129 项 Swift 测试、5 项 Connector 测试、lint、文档门禁和生产打包。
- 发行回归：最新包再次通过 `make verify-core`、`WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make verify-offline`、`make acceptance-local-provider` 和同路径 `make acceptance-offline-model`；Core/Offline DMG 的 `hdiutil verify` 均通过。
- 真实回归：配置快照改动后通过 `make acceptance-interruption`（1.220 秒）；系统声音真实验收当前环境未通过，见本日志顶部边界记录。

## 本机 ASR 服务预设

- 规格：新增 `specs/2026-08-23-local-asr-service-presets.md`，冻结四个 loopback OpenAI-compatible 起点；选择预设不安装、启动、扫描端口或发网络请求。
- 实现：`ASRServicePreset` 提供通用、whisper.cpp、faster-whisper、FunASR/LocalAI 预设；设置页“套用预设”只修改草稿的 ASR 路由、地址和模型，API Key 与 LLM 设置保持独立。
- 验证：预设安全性、唯一性、发现策略和草稿隔离 3 项测试通过；真实服务是否已安装/兼容仍需用户点击健康检查确认。

## 最新门禁与安装回归

- 验证：设置分区保存前的受控权限下 `make verify` 通过 131 项 Swift 测试、5 项 Connector 测试、文档门禁、lint 和生产打包；沙箱内直接运行 Connector 测试会因 Unix Socket `listen EPERM` 失败，已按环境约束重跑并通过。
- 发行回归：`make install` 已安装最新包到 `/Applications/Woice.app`；来源投影修复后 `make verify-core`、`WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make verify-offline`、`make acceptance-local-provider` 和同路径 `make acceptance-offline-model` 均通过，Core/Offline DMG 的 `hdiutil verify` 均为 VALID。
- 状态：本机可完成的本轮预设、配置快照、后台转写耐久化和发行校验切片已完成；完整多语种/长录音与真实桌面矩阵、生产 Catalog、Developer ID/公证和 M2-09 Agent 交接仍未完成。

## 双轨会议转写契约与来源投影修复

- 规格：新增 `specs/2026-08-23-meeting-transcription-acceptance.md`，将标准 `meetingMix` 单请求、来源分离双请求、系统 CAF→WAV 和原始字节不变冻结为本机门禁；不把 loopback Fixture 当成真实会议或第三方服务验收。
- 修复：外部 ASR 路径统一复用 `applyTranscriptionResult`；来源分离模式现在会在原文和时间戳片段中保留“麦克风/电脑声音”来源标签，标准模式仍只保留一份 meetingMix 原文。
- 验证：`make acceptance-meeting-transcription` 通过；分区保存改动后的完整 `make verify` 通过 133 项 Swift 测试、5 项 Connector 测试、lint、文档门禁和生产打包。
- 边界：真实会议应用、长录音、睡眠/设备变化/崩溃矩阵、Developer ID/公证和生产 Catalog 仍待外部验收。

## Catalog 多文件模型下载编排

- 规格：新增 `specs/2026-08-23-catalog-model-download-orchestration.md`，冻结“已验证 Catalog 条目 -> 受控多文件下载 -> 逐文件校验 -> 原子安装 -> 显式切换 Provider”边界；旧 WhisperKit 固定 revision 安装器只在没有匹配 Catalog 条目时作为兼容路径。
- 实现：`ModelPackManifest` 增加签名覆盖的可选 `downloadBaseURL`；新增 `ModelCatalogDownloadCoordinator`，只接受 HTTPS、无凭据、allowlist host，并复用现有 Range 续传、空间检查、SHA-256 和 `ModelPackStore` 原子提交；同大小但哈希不匹配的缓存文件会被删除并重新获取。
- UI：设置页在存在已验证且非内置的 Catalog 条目时显示“发行版模型清单”下载入口；重复点击不会并发下载，取消/失败不切换当前模型。
- 验证：`ModelCatalogDownloadTests` 的多文件 fixture 安装、缺失地址和未允许主机拒绝均通过；`swift test --filter ModelCatalogDownloadTests` 2 项通过。真实生产 Catalog host/key、远程服务和 Developer ID/公证仍未完成。

## M2-08 本机 Provider / Offline 验收入口

- 实现：补齐计划中引用但此前缺失的 `make acceptance-local-provider` 和 `make acceptance-offline-model`；前者覆盖本机 ASR 路由、模型快照和无 Provider 等待状态，后者要求显式模型根目录并验证 bundled inventory、Offline DMG 和 `hdiutil` 校验。
- 验证：`make acceptance-local-provider` 通过；`WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make acceptance-offline-model` 通过。两者仍不替代 Developer ID/公证和干净账户真实安装。

## 升级计划状态同步

- 状态：本轮可本机验证的开发切片已完成；计划仍保持“进行中”，因为完整多语种/长录音与真实桌面矩阵、生产 Catalog、Developer ID/公证和 M2-09 Agent 交接尚未完成。
- 文档：同步 `doc/INDEX.md`、`doc/plan/INDEX.md`、M2-08h、M2-09e 和 PI Extension 规格，明确 `woice.read_material` v1 与 PI `woice_read_material` 已实现，MCP/真实 Agent 仍后置。
- 门禁：文档修改后 `make docs-check` 与 `make harness-check` 均通过；代码侧完整 `make verify` 已通过 123 项 Swift 测试、5 项 Connector 测试及生产打包；随后 `make install`、`make verify-core`、`WOICE_OFFLINE_MODEL_ROOT="/Users/water/Library/Application Support/Woice" make verify-offline` 均通过，当前包已安装到 `/Applications/Woice.app`。

## Catalog 更新链与模型基准矩阵

- 实现：Catalog 新增签名历史重放、密钥轮换/撤销、旧公钥拒绝和历史缺失 fail-closed；新增受限 HTTPS Fetcher，只允许显式 host allowlist、无凭据、JSON、2 MiB 内响应和 10 秒内超时。
- UI：设置页新增“模型清单/检查更新”入口；发行包未配置远程 URL、Catalog ID 和信任根时保持禁用，不会自动联网。
- 实现：模型基准在 `WOICE_ENFORCE_MODEL_BENCHMARK=1` 下要求 `zh/en/mixed/silence/noise` 五类样本和默认 300 秒最短时长；开发模式仍允许短音频调试报告。
- 验证：`make acceptance-catalog` 通过签名、信任根、回滚、轮换、历史缺失、HTTPS 传输和多文件下载边界测试；完整五类模型报告尚未在真实 Large/默认候选上执行。

## R0 录音中断真实验收

- 实现：新增 `make acceptance-interruption`，在已授权真实 Mac 上开始录音后注入 `AVAudioEngineConfigurationChange`，复用生产安全停止流程。
- 验证：真实录音收到配置变化后自动停止，音频文件有帧、Journal 清理、Fixture 本机转写完成；脚本通过（1.216 秒）。
- 边界：真实睡眠唤醒、拔出输入设备、进程崩溃、磁盘不足和长录音仍需单独桌面矩阵，不由通知注入替代。
- 复跑：此前代码曾通过 `make acceptance-interruption`（1.221 秒）和系统声音样本验收；本轮权限诊断变更后的 `make acceptance-meeting` 在当前环境因无可用 ScreenCaptureKit 采集目标未通过，见本日志顶部。

## 会议默认单次转写裁决

- 反馈：用户确认会议默认可直接转写派生的合成音频，避免麦克风与系统声音两轨一律分别推理导致处理时长接近两倍。
- 产品裁决：新安装默认“标准模式”，对齐麦克风 WAV 与系统 CAF 后生成可重建 `meetingMix`，只创建一条 ASR Job；“来源分离”是用户显式选择的高精度模式，才创建两条原轨 ASR Job。
- 原件边界：麦克风 WAV 与系统声音 CAF 仍为不可变真相源；meetingMix 同时服务默认 ASR 和统一回放，可删除、可重建，不替代原轨。
- 准确性边界：标准模式不伪造“我的麦克风/电脑声音”句级来源；重叠讲话、回声或需要来源标签时，用户可改用来源分离重转写，生成新 Transcript Artifact 而不覆盖标准原文。
- 外发边界：外部 ASR 确认必须显示实际文件/请求数；标准模式为 1，来源分离为 2，取消为 0。历史麦克风单轨授权不扩展到 meetingMix 或系统轨。
- 计划合并：决策已直接回写 M2-08a/b/h/i、R2 验收门禁和双轨规格；不新建工作包，M2-08 的 36-54 个工程日权威基线保持不变。
- 实施边界：本轮只更新规格与计划，未修改业务代码，未声称标准或来源分离模式已实现。

## 升级计划合并与冲突消除

- 反馈：新写入的双轨会议与录音升级内容必须合并进原有计划，不能与当前路线图产生第二套工作包、工期或完成状态。
- 冲突核对：当前路线图明确“实施仍使用原工作包 ID”，但新文档出现了 `UP-01...UP-05`、`DT-01...DT-06` 和独立工期，存在并行排期风险。
- 合并裁决：取消 `UP-*` 和 `DT-*` 作为实施编号；`AC-DT-*` 仅保留为规格验收编号。双轨采集/时钟并入 M2-01/M2-03，轨道 Schema、逐轨 ASR 与素材就绪并入 M2-08a/b/h/i，统一回放/合并原文/导出并入 M1-04/M2-03 的 R2 退出检查。
- 权威源：`doc/plan/2026-08-22-current-roadmap-and-plan-transition.md` 继续是唯一跨计划状态源；模型/逐轨 ASR 的实施只在 `doc/plan/2026-08-22-model-integration.md` 维护。竞品升级文档改为验收门禁清单，双轨文档保留产品/技术规格，两者都不单独维护排期。
- 工期合并：M2-08 权威计划在纳入轨道 Schema、音频准备、逐轨路由、合并原文与真实会议验收后，P0 顺序基线由 27-40 个工程日调整为 36-54 个工程日；不再叠加专项规格中已删除的独立估时。
- 边界：本轮只合并计划与文档状态，未修改业务代码，未改变 R0 -> M2-08 -> R2 -> M2-09 的顺序。

## 会议双音轨与合并转写规格

- 反馈：开启系统声音后，当前转写似乎只处理麦克风 WAV；需要冻结会议场景下麦克风、电脑声音、音频原件和会议原文的产品边界。
- 现状核对：系统声音已由 ScreenCaptureKit 独立保存为 CAF；本机整段 ASR、外部 ASR 和分段 ASR 都固定读取 `store.audioURL(for:)` 的麦克风 WAV。麦克风被判定为静音时，即使系统轨有效也不会创建转写任务。
- 产品裁决：历史和详情仍是“一场会议”；存储层保留麦克风 WAV 和系统声音 CAF 两条不可变原件。默认一个统一播放器，使用可删除、可重建的 `meetingMix` 派生文件，不强制合并后删除原轨。
- 转写裁决：每条有效轨独立标准化、创建 ASR Job 并保存来源；有时间戳时按共享 Session 时间线确定性合并，无时间戳时展示“我的麦克风/电脑声音”两个原文区块，不伪造顺序或说话人姓名。
- 隐私边界：本机双轨转写不产生外部请求；外部 ASR 在发送两轨前单独确认目标、数据类型、音轨数和请求数，不沿用过去麦克风单轨授权。
- 规格：新增 `doc/spec/2026-08-22-dual-track-meeting-transcription.md`，定义数据契约、共享时钟、CAF -> WAV 准备层、逐轨 ASR、原文合并、统一回放、部分失败语义、时间表、15 项验收标准与测试矩阵。
- 计划回写：双轨事实和时间对齐进入 R0；音频标准化与逐轨 ASR 进入 M2-08；合并原文、统一回放与开放导出进入 R2。
- 实施边界：本轮仅分析、建立规格并更新计划；未修改业务代码，未声称双轨转写已实现或验收通过。

## 会议双音轨处理链路实现

- 实现：`WoiceCore` 新增 `AudioTrackKind`、`MeetingTranscriptionMode`；`RecordingRecord`、`TranscriptSegment`、`ProcessingTask` 保留轨道来源、模式、meetingMix 和系统轨道时长/偏移，旧 JSON 缺字段按兼容规则读取。
- 实现：新增 `AudioPreparationService`，将 WAV/CAF 受控标准化为 16 kHz 单声道 WAV，按轨道偏移生成可重建 meetingMix；原始麦克风 WAV 与系统 CAF 不覆盖。
- 实现：标准模式默认生成 meetingMix、创建一条 `meetingMix` ASR Job，并在详情页提供统一会议回放；来源分离模式按有效音轨建立两个任务和逐条外发确认。
- 实现：系统音轨外部 ASR 发送前先转换为标准 WAV，避免把 CAF 内容伪装成 `audio/wav`；系统音轨有效而麦克风无声时仍保留录音并允许转写。
- 验证：`swift test` 全部 96 项通过；新增双轨合成、来源分离确认队列、契约兼容测试。真实会议播放和长录音 Journey 仍待人工验收。
- 真实验收：安装后的当前包运行 `WOICE_RUN_APPSTATE_CAPTURE=1 WOICE_REQUIRE_SYSTEM_AUDIO=1 WOICE_REQUIRE_SYSTEM_AUDIO_SIGNAL=1 swift test --no-parallel --filter appStateMeetingModePersistsDualTrackRecord`，测试期间播放 `/System/Library/Sounds/Funk.aiff`，15.465 秒 Journey 通过并确认系统信号、meetingMix 16 kHz 单声道和 `meetingMix` Job；视频/会议应用与长录音仍需用户桌面复验。

## 开放素材导出与录音中断安全

- 实现：录音详情新增导出菜单，可分别导出麦克风 WAV、电脑声音 CAF、会议合成 WAV、规范化原文 TXT、时间戳 JSON 和 Markdown；导出目录沿用设置，文件均为派生副本，不覆盖原始音频或 RecordingRecord。
- 实现：JSON 导出包含录音 id、创建时间、时长、规范化原文、带来源的时间戳片段和原始音轨文件清单；无原文时明确返回“请先完成转写”。
- 实现：监听 `AVAudioEngine` 配置变化和 `NSWorkspace.willSleepNotification`，录音中发生输入路由变化或即将休眠时复用同一 stop 流程安全保存，避免 UI 假显示录音。
- 验证：新增开放导出测试，确认音频字节与 RecordingRecord 在导出前后不变；`AppStateRecordingIntegrationTests` 7 项通过，素材导出测试通过。真实休眠、设备拔出和进程崩溃矩阵仍需真实 Mac Journey。

## 模型下载控制

- 实现：设置页活动模型按钮可取消下载；取消会沿用现有可恢复缓存并将 SQLite 任务置为 `paused`，非活动 Tiny/Large 不再显示为同时下载。
- 实现：模型库存支持删除已下载、非当前使用且非安装指针的版本；随包模型、当前模型和安装指针版本均 fail-closed，不影响录音、Transcript 或 Job 快照。
- 验证：新增“只允许删除非当前下载版本”测试；模型库存相关 4 项测试通过。完整 `make verify` 通过：123 项 Swift 测试、5 项 Connector 测试、lint、文档门禁和生产打包均通过；最终包已重新构建，安装目录仍为 `/Applications/Woice.app`。

## Large 模型真实重转写验收

- 验证：`WOICE_RUN_LARGE_WHISPERKIT=1 swift test --no-parallel --filter installedLargeWhisperKitPackTranscribesExistingWAV` 在当前机器完成，已安装 Large 模型对已有 WAV 的本机重转写通过，单测耗时 223.274 秒。
- 边界：该证据只确认 Large 已安装包可加载并完成一次真实 WAV 推理，不替代中文/英文/混合/噪声基准、RTF/内存测量、默认模型冻结和发布签名公证。

## Provider 配置迁移与 Registry

- 实现：新增 `ASRProviderConfiguration` 统一承载选择方式、Endpoint、模型和派生的数据位置/传输方式/Provider ID；旧 `AppSettings` 字段保留兼容门面，旧设置文件读取时迁移，新编码不写入 ASR API Key。
- 实现：新增有限可信 `ASRProviderRegistry`，注册 macOS Speech、WhisperKit 和 OpenAI-compatible ASR，发布能力、健康状态和数据位置；设置页增加“转写能力状态”区，不会因查看设置加载模型或发网络请求。
- 验证：Provider 配置迁移、局域网/云端位置识别、Registry 注册/解析/fail-closed 目标测试通过；最新完整门禁已通过 123 项 Swift 测试。

## 签名模型 Catalog 验证

- 实现：新增 `ModelCatalog` 与 `ModelCatalogVerifier`，Catalog 固定 schema 版本、稳定 ID、生成时间和完整 `ModelPackManifest` 条目；条目重复、未知 schema、无签名、错误算法、错误公钥或签名不匹配均 fail-closed。
- 安全：使用 CryptoKit Ed25519 验证签名，Catalog 验证只接受显式信任的 key ID；验证通过前不进入模型下载或安装流程。当前只落地验证原语，不把测试密钥当成生产信任根。
- 验证：Catalog/传输/轮换/下载相关测试已覆盖合法签名、篡改拒绝、缺失签名、错误 key、重复模型包、受信历史重放、版本回退、密钥撤销、HTTPS host allowlist、重定向、内容类型、响应大小、多文件安装和同大小损坏文件重试；最新完整 `make verify` 通过 123 项 Swift 测试、5 项 Connector 测试。
- 待办：生产公钥/host 配置、多文件真实下载编排、远程撤销后的库存 UI 和正式发布门禁。

## 升级计划完成切片：权限、首启和发行校验

- 修正：设置页 Provider 状态行现在显示真实的数据位置和传输方式；模型发现、粘贴、录音会话和素材打开失败反馈不再显示字面量 `(value)`，而是显示实际值。
- 修正：系统声音权限请求后在有限窗口重新读取 `SCShareableContent`，并在应用回到 active 时继续刷新；运行时可共享内容优先于过时的 preflight 状态，避免已经授权仍显示“需要权限”。
- 实现：菜单栏首次启动引导改为三步可跳过流程，明确录音、本机保存、转写和外发确认；第二步按当前 Registry inventory 显示本机/外部能力，不自动请求权限或网络探测。
- 实现：新增 `scripts/package_dmg.py` 与 `package-dmg-core/offline`、`verify-core/offline` 入口。当前机器实际生成并通过 `hdiutil verify`：Core DMG 约 3 MB，Offline（Large 模型）DMG 约 458 MB；两者仅为 ad hoc 本地包，不代表 Developer ID/公证。
- 实现：新增显式模型基准入口 `scripts/run_model_benchmark.sh` / `make model-benchmark`，按音频类别、RTF、峰值常驻内存、空输出和可选参考文本 WER 写入 JSON，不设置环境变量时不会运行或下载模型。
- 验证：Tiny 短英文音频真实报告已生成：音频 2.17 秒、RTF 0.51、峰值常驻内存约 817 MB；这不是计划要求的 5 分钟多语种/静音/噪声默认模型冻结证据。

## 本机 ASR 模型发现

- 实现：外部 ASR 设置新增用户主动“发现本机模型”，只读取 loopback/RFC1918 `/v1/models`，支持 `data[].id` 和 `owned_by`，发现结果只写入设置草稿。
- 安全：公网 Endpoint、自动启动探测和录音上传均 fail-closed；请求超时上限 5 秒，API Key 只作为请求头使用。
- 验证：loopback 列表解析、公网拒绝和零请求边界测试通过。

## R2 素材外部打开

- 实现：录音详情“打开素材”菜单支持 Finder 显示全部已保存音频，以及用默认播放器打开麦克风、电脑声音和 meetingMix；不存在的音轨自动禁用。
- 安全：动作只读取当前 Recording 已存在的路径，不导出、不复制、不修改 Recording/Job；缺失文件 fail-closed。
- 验证：缺失文件 fail-closed 测试通过；真实 Finder/默认播放器桌面 Journey仍待验收。

## R2 录音搜索投影

- 实现：Workspace 素材库和兼容 HistoryView 共用 `recordingMatchesSearchQuery`，支持标题、规范化原文、Markdown、日期、素材状态和系统音频/会议回放来源；多词查询采用 AND 语义。
- 边界：搜索只读取已加载 Recording，不触发转写、网络、文件扫描或 Agent。
- 验证：共享投影测试通过，确认 Whisper 控制 token 不参与搜索且部分命中不会返回。

## R0 录音会话中断恢复

- 实现：录音开始前原子写入 `recording-session.json`；正常提交或显式取消清理；启动时验证 WAV/CAF 容器和时长，至少一条有效音轨即可恢复 Recording。
- 安全：恢复只保存素材事实，不自动外发、不生成伪造原文；无可读音频时只清理 Journal，原始字节不覆盖。
- 验证：有效 WAV 恢复、无效 Journal fail-closed 和 SHA-256 不变测试通过；真实崩溃/长录音/磁盘异常 Journey 仍待验收。

## 素材就绪状态投影

- 实现：`WoiceCore` 新增 `RecordingMaterialStatus`，从持久化原文和转写任务确定性投影“已保存、等待模型、处理中、素材已就绪、部分原文已就绪、转写失败但素材安全”。
- 实现：素材库列表与录音详情使用同一状态投影，重启后不依赖页面内存状态；来源分离一轨成功/一轨失败时可表达部分就绪。
- 验证：新增状态投影契约测试；目标测试通过。完整门禁在本轮状态 UI、RPC 和下载控制改动合并后重新执行。

## 只读素材 RPC 投影

- 实现：新增 `woice.read_material` 只读方法，返回稳定 `artifact_id`、素材就绪状态、规范化原文、时间戳片段和已持久化音轨文件清单；原文/片段输出各受 64 KiB 上限保护。
- 边界：该方法不发网络、不触发转写或 Markdown、不改变 Job、Keychain 或权限；仍通过现有 Unix Socket 和协议校验。
- 验证：PI Router、协议和 Connector Node 测试通过，读取素材时 `pendingExternalProcessing` 保持为空；PI 只读工具已暴露，MCP 搜索/分页和真实 Agent 仍后置。

## 真实会议验收脚本

- 实现：新增 `make acceptance-meeting`，脚本在真实 Mac 上播放 `/System/Library/Sounds/Funk.aiff`，并运行双轨 AppState 验收；不使用隐私录音 Fixture。
- 历史验证：此前一次环境中 `make acceptance-meeting` 曾通过（15.406 秒），确认系统音频非静音信号、CAF 可读、meetingMix 16 kHz 单声道和标准模式单条 `meetingMix` Job；本轮环境结果以日志顶部的失败边界为准。脚本已改为循环播放，避免短音频在 SwiftPM 编译期间结束造成假失败。
- 边界：视频/会议应用、休眠/设备拔出、长录音、崩溃和 TCC 变更仍未由该脚本覆盖。

## MSS 最终自动门禁与稳定签名覆盖安装

- 将首启引导从旧菜单栏视图迁入唯一工作台右上角；菜单栏运行时继续只提供四个快速动作，不恢复最近结果、导入、文字转音频或“更多”菜单。
- `make verify` 通过：文档/Harness、PI/MCP 连接器、170 项 Swift 测试（10 suites）、Swift 格式门禁和 Core 包构建均通过；设置独立保存、快捷键冲突预检/回滚、会议转写来源和本机服务自动继续的专项门禁也通过。
- 使用同一 Apple Development 身份 `Apple Development: jianglewis@126.com (23RRB32DPL)`，Team ID `C27W5WB6R8`，生成并依次覆盖安装最终 Build A `2026082307` → Build B `2026082308`；Bundle ID、四项 TCC Usage Description、entitlements、designated requirement 和 Team ID 均一致。
- 稳定签名证据目录：`/private/tmp/woice-stable-ab-20260823-mss-final3`；当前 `/Applications/Woice.app` 为 Build B `2026082308`。脚本未调用 `tccutil reset`，TCC 授权连续性、真实录音、系统声音、Dock/Finder 视觉、VoiceOver 和跨应用快捷键冲突仍需按 `specs/2026-08-23-manual-acceptance-runbook.md` 手动记录。

## 转写模型路由单一真相源

- 根因：当前用户设置同时保存了 Large 本机模型和显式 `external` 路线；录音任务因此使用 `openai-compatible.asr / whisper-1 / 127.0.0.1:8080`，而端口没有服务监听，产生 `Could not connect to the server.`。麦克风 WAV、系统 CAF 和 meetingMix 均可读，故障不在采集。
- 实现：新设置默认选择本机模型；旧 `automatic` 解码为本机且不再因残留 Endpoint 隐式切换。下载、Catalog 安装、目录导入或切换已安装模型都会同步激活本机路线；套用/保存自定义服务仍显式使用用户 Endpoint 和模型。
- 实现：设置页收敛为“本机模型（推荐）/自定义服务”，新增“实际转写路线”；模型库存只有在路线和版本同时匹配时显示“当前使用”，外部路线下可重新选择同一本机版本。
- 验证：新增默认路线、兼容迁移、模型切换、外部失败任务切回本机重试测试；`make test` 通过 175 项 Swift 测试、10 个套件。任务重试会把 Provider/模型/版本/数据位置刷新为实际本机模型，且不再进入 HTTP 端口确认。
- 完整门禁：修复旧外部授权队列恢复后再次执行 `make verify`，文档/Harness、PI 6 项、MCP 2 项、lint、175 项 Swift 测试（10 个套件）和生产构建打包全部通过。
- 安装验证：以同一 Apple Development 身份和 Team ID `C27W5WB6R8` 生成稳定签名 A `2026082321` → B `2026082322` 并覆盖安装；严格签名验证通过，证据目录为 `/private/tmp/woice-stable-ab-20260823-transcription-route-final`，当前 `/Applications/Woice.app` 为 B。
- 运行时验证：通过已安装 App 的设置页把转写方式切为“本机模型（推荐）”，实际目标显示 `WhisperKit openai-whisper-large-v3-v20240930-626mb`、revision `0f63a7800b00dd0226abd051b906c246e1907482`；保存的 `selection` 为 `onDevice`。原 `127.0.0.1:8080` 仅作为未启用的自定义服务草稿保留，不参与转写，处理任务页也不再恢复旧 HTTP 授权卡片。
