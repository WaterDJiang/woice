# 2026-09-01

## 三声道麦克风 AAC 写入 `-50` 修复

- 系统日志确认失败录音的 HAL 输入为 `3 ch, 48000 Hz, Float32, interleaved`，主文件编码器为 `2 ch, 48000 Hz AAC`；首帧直接写入时 `ExtAudioFileWrite` 返回 `com.apple.coreaudio.avfaudio error -50`。这不是麦克风权限、磁盘或转写模型错误。
- `AudioWriter` 现在在单一边界把任意麦克风 PCM 规范化为同采样率、Float32 非交错格式；超过 2 声道时下混为 2 声道。主 AAC、VAD 片段、实时预览和滚动恢复块复用同一份规范化缓冲，避免两条写入路径再次漂移。
- 新增确定性 3 声道交错 Fixture，验证主 M4A 首帧可写、恢复块尾部可提交且均可重新读取。真实麦克风测试默认不再自动采音，只在显式设置 `WOICE_REQUIRE_MIC_AUDIO=1` 时执行。
- `make test` 通过 269 项 Swift Testing 与 18 项 XCTest；`make verify` 通过文档、Harness、PI/MCP、Swift 测试、lint、资源和 Xcode Dev 构建门禁。真实目标麦克风验收必须由用户在安装包中点击可见录音按钮触发。
- 首次签名复核在受限环境中返回 `CSSMERR_TP_NOT_TRUSTED` 并错误报告 `0 valid identities found`；在本机权限下复核确认实际有 2 张 Apple Development 证书和 7 个有效代码签名身份。随后使用有效 Apple Development 身份重新构建并覆盖 `/Applications/Woice (Dev).app`，`codesign --verify --deep --strict` 通过；Bundle 为 `0.1.4 (5)` / `com.woice.app`，非 Ad Hoc 且 Team ID 存在。未启动 App、未重置 TCC、未触碰用户素材。

## Qwen 粤语乱码与行内重复修复

- 用户真实粤语转写回归暴露两个遗漏：`QwenOutputParser` 只修复同时含 direct-high/fallback byte 的序列，所以 `ä½¢` 没有恢复为 `佢`；重复治理只处理完全相同行，漏掉了行内的 `讲讲讲…` 和 `唔该，唔该…`。
- byte 修复改为以 ASCII 为安全边界，仅将可严格解码的连续高字节序列恢复为 UTF-8；无法解码的字符保持原样，波兰语合法 Unicode 回归仍通过。
- 增加确定性行内重复折叠：单字连续 8 次、长度 2～16 的短模式连续 5 次才触发，保留首份；未达阈值和非相邻重复不变。每个 30 秒块与最终合并文本都执行同一门禁。
- 识别语言下拉表新增“粤语（yue）”，Qwen 传入 `Cantonese`；“自动检测”仍为首项。
- 验证：`QwenOutputParserTests` 15 项、语言选项 3 项定向回归通过；`make verify` 通过 268 项 Swift Testing 与 18 项 XCTest、PI/MCP、文档/Harness/lint 和 Xcode 无签名构建，`git diff --check` 通过。未使用用户原始音频，真实粤语重转写仍待安装包人工确认。
- 覆盖安装尝试按项目门禁失败：`WOICE_LOCAL_SIGNING_IDENTITY 未设置；拒绝安装 ad hoc Dev App。`未用 Ad Hoc 签名绕过，未替换当前安装 App，未触碰用户数据或 TCC。
- 用户明确要求覆盖安装后，从旧 `/Applications/Woice (Dev).app` 解析并复用同一 Apple Development 签名身份，通过 `make install` 完成原位替换。安装后确认 `0.1.4 (5)`、`com.woice.app`，`codesign --verify --deep --strict` 通过，不是 Ad Hoc 且 TeamIdentifier 存在；未启动 App，未重置 TCC，未触碰用户素材。

## 覆盖安装重新转写修复包

- 使用本机已有 Apple Development 签名身份重新构建并覆盖安装 `/Applications/Woice (Dev).app`；未触碰正式版 App、用户数据或 TCC。
- 安装包字段确认：`0.1.4 (Build 5)`、`com.woice.app`；`codesign --verify --deep --strict` 通过，Authority 与 TeamIdentifier 均存在，非 Ad Hoc 签名。
- 这是当前 Build 5 的修复重建，未修改版本号；未启动 App，真实“重新转写”桌面验收由用户手动完成。

## 本机已完成任务重新转写修复

- 新增规格 `specs/2026-09-01-local-retranscription-completed-task.md`：详情页重新转写需保留旧 Artifact，并在同一素材级幂等键下启动新一轮本机任务。
- 根因是本机路径复用已完成任务的 Lease 键，而 SQLite Lease 只允许非 `completed` 状态；现在取得 Lease 前会将非 `running` 任务显式置为 `queued`，再走既有 Lease、运行和 Artifact 追加流程。
- 新增 `completedLocalTranscriptionCanBeExplicitlyRetried` 回归：已完成任务重新转写调用 Provider 一次、追加新 Artifact、保留旧原文和原始音频；重复点击不重复启动。修复前定向测试按预期失败，修复后通过。
- 未安装新包、未启动 App、未重置 TCC；真实桌面验收按用户要求由用户手动完成。

## 本机安装 Dev Build 5

- 按用户要求用本机钥匙串已有的 Apple Development 身份重新构建并覆盖安装 `/Applications/Woice (Dev).app`；未触碰 Store App 或用户数据。
- 安装后 Bundle 字段确认：`0.1.4 (Build 5)`、`com.woice.app`；`codesign --verify --deep --strict` 通过，Team ID 非空。
- 未启动 App、未重置 TCC；真实桌面和录音体验由用户手动验收。

## 新增素材质量开发计划并结项旧 WCL

- 新建 `doc/plan/2026-09-01-material-naming-durability-detail-performance-qwen-quality.md`，状态为待实施；覆盖自定义标题、导入文件名、块级异常恢复、详情首屏性能和 Qwen 输出质量。
- 根因结论：当前固定 `qwen3-asr-swift` tokenizer 对每个 BPE token 单独做 UTF-8 解码；中文 UTF-8 字节跨 token 时产生截图中的 byte-unicode 乱码。Qwen 正式推荐在 tokenizer、协议解析、重复和性能门禁通过前保持关闭。
- 计划迁移：旧 WCL 计划结项为历史；WCL-07 的输出正确性/模型矩阵迁入 MRQ，WCL-04 Developer ID 和 WCL-06 Store 外部条件继续由发行路线与 MAS 专项承接。
- 本轮只写计划和迁移状态，未修改产品代码、数据库或用户素材，未运行产品测试。

## TestFlight 0.1.4 Build 5 上传

- 发布内容：包含工作台侧栏恢复、模型库存去重、首次模型提示、内存推荐和 Dev/Store 隔离；Build 从 4 升至 5，Store 包继续为 `Woice` / `com.water.woice` / Store Channel。
- 包体边界：Archive 为 arm64 + x86_64；`DistributionManifest` 的随包模型列表为空，App Resources 不含 `Models` 或 `BundledModels`。
- 自动门禁：完整 `make verify` 通过 Swift 237 项、PI 7 项、MCP 2 项；首次受限运行因 Unix Socket `EPERM` 失败，切换到本机环境原样复跑通过。
- 签名说明：Xcode 自动签名 Archive 与 App Store 导出成功。本机 `pkgutil` 仍将 Installer 证书链报告为不受信，与上一 Build 的钥匙串状态一致；未把本机结论冒充为 Apple 校验结果。
- 上传结果：`xcodebuild -exportArchive` 完成 App Store Connect 分析和 100% 上传，明确返回 `Upload succeeded`、`Uploaded Woice-Store`；Apple 当前显示 `Uploaded package is processing`，尚不宣称 TestFlight 已可安装或 App Store 已发布。
- Catalog 边界：既有 Ed25519 发布私钥仍缺失，没有生成替代信任根。Build 5 使用当前线上已签名 Catalog；Qwen 目标档位不可下载时安全回退到 Catalog 中可下载的模型。
- 清理：完整验证产生的顶层与 Derived Products Dev App 已删除；Build 4 旧 Archive 已带时间戳移入废纸篓，只保留 Build 5 当前 Archive，不触碰 `/Applications/Woice (Dev).app` 或用户数据。

## MRQ-01/MRQ-02 代码切片与回归

- 规格：新增 `specs/2026-09-01-material-quality-and-naming.md`，并将活动计划状态更新为“MRQ-01/MRQ-02 代码切片与自动测试完成；MRQ-00、MRQ-03、MRQ-04、MRQ-05 待实施/待真实验收”。
- Qwen：新增 `QwenOutputParser`，在 Provider 边界把 GPT-2 byte-level 映射还原为连续 UTF-8，清理 `language/<asr_text>/<|...|>` 协议内容，只移除相邻确定性重复，并在替换字符或 byte 残留时拒绝提交为可用原文；Qwen3-ASR Provider 已接入该门禁。未修改第三方 Runtime，未解锁正式 Qwen Catalog 推荐。
- 素材命名：`RecordingRecord` 增加可选 `userTitle` 和统一 `displayTitle`；导入素材优先保留原始文件名及扩展名；详情支持原位重命名，侧栏提供右键入口；搜索、Markdown、JSON、RPC、导出文件名统一使用 `displayTitle`，旧 JSON 保持兼容。
- 验证：`swift test --no-parallel --filter QwenOutputParserTests --filter MaterialTitleTests --filter MediaImportTests` 通过；`make test` 通过，241 项 Swift 测试、16 个 Suite 全部通过；`make format` 和 `git diff --check` 通过。首次受限测试因 Swift ModuleCache `Operation not permitted` 失败，切换到本机授权环境原样复跑通过，未将环境失败归因于代码。
- 未完成：MRQ-00 基线测量、MRQ-03 十秒滚动块/Manifest 故障恢复、MRQ-04 摘要/详情异步加载与长素材性能矩阵、MRQ-05 多 Channel/真实 Mac/发行收口；本轮没有安装或发布新 App。

## MRQ-00～MRQ-05 自动门禁收口

- MRQ-03：录音链路新增约 10 秒 AAC/M4A 滚动块、原子 Manifest、块文件 `fsync`/原子改名、后台 SHA-256 与清单提交；启动恢复会收编已提交孤儿块、隔离 `.partial`/损坏块，并按已校验块重建规范化音频，不覆盖原始文件。
- MRQ-04：素材列表使用 SQLite 摘要投影；详情按 ID 异步读取并投影最新 Job 状态；增加 5 条 LRU、原文后台规范化与 1200 字符分块懒渲染、音频元数据缓存，避免列表/详情首屏解码完整音频或一次性创建长文本视图。
- MRQ-00：新增不含用户内容的可重复 Fixture 与报告。最新报告 `build/material-benchmark-20260901.json` 的 500 条摘要加载 P50/P95 为 `0.58/0.63 ms`；详情冷读为 `0.17/0.31 ms`；60 分钟 AAC 元数据冷读为 `6.18/6.56 ms`；10,000 段详情冷读为 `18.89/19.42 ms`，均低于当前 400/500 ms 预算。
- 自动验证：`make format`、`make lint`、`make docs-check`、`make harness-check`、`git diff --check`、`make test`（259 项 Swift Testing、12 项 XCTest，16 个 Suite）、`make verify`、`make xcode-build-store` 和 `make verify-app-store` 均通过；Store Bundle 检查确认不携带模型。
- 未冒充完成：真实 Mac 的 `SIGKILL`/突然断电/磁盘写满/双轨单边故障、长素材 SwiftUI 主线程 Hang 与内存曲线、Qwen 官方对照/300 秒与 60 分钟准确率、稳定签名覆盖安装和 Developer ID/公证/签名 Catalog 仍需目标环境验收。

## Qwen 本机运行时尾块回归与公开样本烟测

- Qwen 基准现支持显式 `WOICE_BENCHMARK_INCLUDE_QWEN=1`，并自动依赖 `make xcode-build-store` 产出的 MLX `default.metallib`；缺少 shader 或模型清单时 fail-closed，不伪造通过。
- 本机 Qwen3-ASR `0.6B 4-bit` 已真实加载 tokenizer、音频塔、文本解码器和权重。对上游公开 20 秒英文样本，Woice Provider 输出与参考文本 WER 均为 `0`；两次运行 RTF 为 `0.028～0.082`，峰值 RSS 为 `约 845 MiB`。报告：`build/qwen-quality-natural-wer-2-20260901.json`。
- 诊断确认：24 kHz 输入标准化后的 16 kHz 文件包含完整主块以及 `512～768` 帧的短尾块；尾块峰值约 `0.0063`，属于容器/转换静音尾，不是有效语音丢失。`QwenAudioSignalDetector` 只跳过短的最终静音尾块；整段静音仍返回空结果失败，不生成 ready Artifact。
- 解析器新增合法 Unicode 与 `language None<asr_text>` 空协议回归，并收窄 byte-level 修复条件，避免把合法字符误改成控制字符；`QwenOutputParserTests` 现为 9 项且通过。`AudioPreparationServiceTests` 的重复标准化回归也通过。
- macOS `say` 合成中文/英文素材无法作为质量准入证据，完整中文、中英混合、静音、噪声、300 秒、60 分钟及官方 Python/Swift 对照仍待；真实 `SIGKILL`/掉电/磁盘写满/双轨单边与 SwiftUI 长时验收仍待目标 Mac。

## 稳定签名 A/B 静态连续性校验

- 在不安装、不重置 TCC、不触碰用户数据的隔离目录中，使用本机 Apple Development 稳定签名身份生成 A/B 两个版本包。
- 自动校验通过：同一 Bundle ID、非空且一致的 Team ID、Designated Requirement、Entitlements 与麦克风/屏幕录制/辅助功能权限声明一致，且 `CFBundleVersion` 按 A→B 递增。
- 结果只证明覆盖安装所需的签名与权限声明具备连续性条件；真实 A→B 安装、重启、TCC 保留和录音 Journey 仍需在目标 Mac 手工执行。本轮没有安装新包，也没有执行 `tccutil reset`。

## MRQ 专项验收复跑

- `make acceptance-material`、`make acceptance-recovery`、`make acceptance-meeting-transcription` 和 `make acceptance-settings` 均通过，覆盖素材搜索/只读投影、Journal/SQLite 恢复契约、双轨会议转写请求边界以及设置分区与 Keychain 隔离。
- `acceptance-recovery` 新增隔离子进程 SIGKILL：子进程先提交 M4A 块再被父测试强制终止，恢复端收编并验证已提交块 SHA，未完成 partial 块进入隔离区。
- 新增门禁后 `make verify` 重新通过：259 项 Swift Testing、12 项 XCTest、PI/MCP 契约、资源/模型清单和 Dev 构建均成功。
- 这些是本机自动契约验收，不替代真实 `SIGKILL`/掉电/磁盘写满、TCC 连续性、长素材 UI 和完整 Qwen 质量矩阵；未安装新包，也未触碰用户素材。

## Qwen 官方中文短样本与转换尾块修复复验

- 从 Qwen 官方示例地址取得公开 `asr_zh.wav`/`asr_en.wav`，只存放在 `/private/tmp`，未写入仓库或用户素材；`afinfo` 确认中文 16 kHz、约 4.2 秒，英文 48 kHz、约 15.1 秒。
- 根因：`AVAudioConverter` 在有效中文块后暴露约 703 帧的非零容器尾块；尾块峰值约 `0.0052`、RMS 约 `0.0034`，模型返回空文本，原实现把该空块当成整段失败并丢弃前面已识别内容。
- 修复：`QwenAudioSignalDetector` 对不超过 2,048 帧的最终块增加峰值门禁，拒绝非语音 DC/噪声尾；保留普通短而有声块，整段静音仍 fail-closed。新增非零尾块回归测试。
- 自动语言路径复验：Qwen3-ASR 0.6B 4-bit 官方中文/英文样本均返回非空结果，RTF `0.076～0.137`，峰值 RSS 约 `842 MiB`；官方样本没有可信逐字参考，未宣称中文 WER。报告：`build/qwen-quality-official-20260901.json`。
- `QwenOutputParserTests` 增至 10 项；后续仍需官方 Python/Swift 对照、中英混合/静音/噪声、300 秒/60 分钟质量矩阵及上游 Runtime 修复/最小 fork 审计。

## 最新源码全门禁与 Store/Qwen 复验

- 最新源码下 `make test` 通过 259 项 Swift Testing、13 项 XCTest；`make lint`、`make docs-check`、`make harness-check` 和 `git diff --check` 均通过。
- `make verify` 通过，重新生成并校验 Dev 构建；`make verify-app-store` 通过，Store 工程与零模型发行包门禁通过。
- 使用当前源码重新运行本机 Qwen3-ASR 0.6B 4-bit 官方中文/英文短样本自动语言烟测，均非空；最新报告 `build/qwen-quality-official-20260901.json` 的 RTF 为 `0.057～0.125`、峰值 RSS 约 `839 MiB`。
- 本轮仍未安装 App、覆盖用户包、重置 TCC、提交或推送；真实 Mac 故障/覆盖安装、完整 Qwen 质量矩阵和正式签名仍按活动计划保留。

## 导入转写浮窗关闭与真实 WhisperKit 录音验收

- 针对导入音视频点击“转文字”后浮窗阻塞其他操作的问题，新增 `specs/2026-09-01-media-import-background-transcription.md`；运行或等待授权时明确显示“关闭并后台继续”，支持 Escape/取消快捷键、VoiceOver 和帮助文案，关闭只改变窗口呈现，不取消已持久化任务。
- `swift test --no-parallel --filter MediaImportTests` 在本机授权环境通过，12 项导入/浮窗/原件安全回归全部通过；受限环境首次运行的原文失败为 `ModuleCache: Operation not permitted`，未归因于代码。
- 最新源码复跑 `make test` 通过（259 项 Swift Testing、13 项 XCTest）；`make lint`、`make docs-check`、`make harness-check` 和 `git diff --check` 通过。
- `make acceptance-whisperkit` 在本机通过：固定 WhisperKit Tiny 模型安装完成，随后使用隔离 Workspace 完成真实麦克风录音 → WhisperKit Provider → 本地素材持久化与转写闭环；转写耗时约 232.6 秒，未记录用户原文，未重置 TCC。
- 首次真实验收曾因 SwiftPM 宿主没有 `NSSpeechRecognitionUsageDescription` 且读取用户开启的实时预览而以 `SIGABRT` 失败；现已改为注入已安装 WhisperKit Provider、隔离临时 Workspace，并显式关闭实时预览/系统音频后通过。该修复不改变正式 App 的权限声明。
- 仍未冒充完成：真实浮窗视觉手感、TCC 连续性、录音进程 `SIGKILL`/掉电/磁盘写满、长素材 UI 与完整 Qwen 质量矩阵继续按活动计划验收。

## 稳定签名覆盖、启动与桌面导入复验

- 使用本机已安装且未写入仓库的 Apple Development 身份完成 Dev A→B 覆盖安装；同 Bundle ID、Team、权限声明和版本递增门禁通过，未执行 `tccutil reset`，未触碰正式版数据/Keychain。
- `WOICE_RUN_LAUNCH_JOURNEY=1 make acceptance-launch-window` 通过：覆盖后的 `Woice (Dev).app` 可真实启动/重开，保持单进程和工作台窗口可见。
- `WOICE_RUN_MEDIA_IMPORT_JOURNEY=1 make acceptance-media-import-desktop` 通过：隔离素材导入、原件保存和本机转写任务链路可完成，临时 Workspace 已清理。
- 这些结果把 MRQ-02/MRQ-05 的真实覆盖、启动和导入证据前移；当前用户数据的 TCC 连续性、长时故障/双轨和正式发行签名仍未静态冒充完成。

## 真实系统声音与默认双轨任务复验

- 首次 `make acceptance-meeting` 暴露两条过时验收断言：实际会议合成是 48 kHz 单声道 AAC/M4A，默认 `sourceSeparated` 任务分别指向麦克风/系统音频，而不是把 `meetingMix` 当作唯一 ASR 任务；系统声音本身已采集成功。
- 按当前产品规格修正验收契约：会议合成是复听/导出派生物，送 ASR 前才临时转换为 16 kHz；默认模式保留会议合成，同时按可用原轨建立转写任务，`standardMix` 才使用单一合成任务。
- 修正后 `make acceptance-meeting` 通过（QuickTime 临时播放源、ScreenCaptureKit 有效峰值、系统音频文件可读、会议合成可读、来源分离任务正确）；临时播放文件已清理。
- 仍需当前安装包在用户桌面上的 TCC 连续性、双轨单边故障和长时会议手测；本次不记录用户原文。

## 录音故障保留语义与事务回滚补强

- 最终容器不可读、音轨写入失败或没有可用完整文件时，不再无条件删除滚动录音材料：只要已有已提交块，就保留 Session Journal、Manifest 和 `.committed.m4a`，下次启动继续恢复；没有可恢复块才清理空会话。
- `RecordingChunkManifestStore.commit` 在原子 Manifest 写入失败时恢复此前的内存快照；已提交文件保留为可收编孤儿，避免界面内存状态先于磁盘事实。
- `AppState.stopRecording` 在 SQLite Recording 事务失败时撤回未持久化的内存素材，保留录音文件、Session Journal 和滚动 Manifest，并以失败态提示重启恢复；不再继续启动转写或把乐观状态展示为已保存。
- 双轨异常恢复如果麦克风文件缺失但系统音频仍可读，会把系统轨切换为主音频引用，标记来源分离，不伪造麦克风或会议合成文件。SQLite `BEFORE INSERT` 故障注入确认 Recording、Job 与摘要投影在事务失败后全部回滚。
- 摘要列表异步 hydrate 期间新增录音/导入门禁：按钮和 AppState 同时 fail-closed，避免空的内存摘要快照覆盖已有 SQLite；新增集成回归覆盖 250 条素材场景。
- hydrate 失败路径继续保持 fail-closed，不会因为清除 loading 标记而重新开放录音/导入；工作台保留摘要列表、展示诊断原因，并提供显式“重新读取”入口。
- 新增 Manifest 权限故障、双轨仅系统音频恢复、SQLite 事务回滚、hydrate 门禁和 hydrate 失败回归；`make test` 通过 264 项 Swift Testing、13 项 XCTest，`make verify`、`make lint`、`make docs-check`、`make harness-check` 和 `git diff --check` 均通过。
- 最新 Store 组合根验证：`make xcode-build-store` 与 `make verify-app-store` 均通过，Store Bundle 仍不携带模型。
- 这次只完成自动故障语义和契约验证；真实 Mac 的突然断电、磁盘写满、长时尾部损失与 TCC 连续性仍未冒充通过。

## Qwen 五类 300 秒严格基准复验

- 使用项目自带的 macOS `say`/FFmpeg 合成夹具运行五类各 300 秒模型基准；夹具只在临时目录生成，不读取用户素材。
- Qwen3-ASR 0.6B 4-bit 的中文、英文和中英混合语音均返回非空结果，RTF 分别约 `0.056`、`0.064`、`0.059`，峰值 RSS 约 `899 MiB`；合成参考 WER 约 `0.615`、`0.099`、`0.323`，因循环语音边界仅作诊断，不作为真实准确率结论。
- Qwen 对静音和噪声返回空结果并保留原始音频，符合 fail-closed 语义；当前严格门禁仍按既有契约将空输出记为失败，因此 `run_model_benchmark.sh --strict` 以 `ModelBenchmarkTests.swift:223` 失败退出。
- 同一矩阵还观察到 WhisperKit Large/Tiny 对静音和噪声返回非空结果，属于待评估的幻觉/无语音质量问题；本次没有把失败改写成通过，也没有解锁 Qwen 正式 Catalog 推荐。

## 素材详情 hydrate 的全入口变更门禁

- 复核发现，摘要列表在 hydrate 期间仍可通过右键/滑动触发删除或重命名；若只加载一条详情后沿用全量 `persistRecordings()`，可能把其余素材从 SQLite 覆盖掉。
- 新增统一 `canMutateRecordings` gate：hydrate 进行中或失败时，录音、导入、重命名、删除、转写、Markdown、切换原文版本、重试处理和任务状态写入均 fail-closed；键盘快捷键和 AppState 直调用同样受保护，完整详情成功后才恢复。
- 工作台摘要列表的导入、删除、重命名和滑动操作同步显示禁用态；hydrate 失败仍保留摘要、诊断信息和“重新读取”入口。
- `AppStateRecordingIntegrationTests` 扩展故障回归：故意损坏一条详情后，采用单条详情也不能重命名或删除，SQLite 摘要仍保持 250 条。最新 `make test` 通过 264 项 Swift Testing（16 个 Suite）和 13 项 XCTest；`make verify`、`make xcode-build-store`、`make verify-app-store`、`make docs-check`、`make harness-check`、`make format` 和 `git diff --check` 均通过。

## 最新源码最终门禁复跑

- 在导入转写浮窗后台继续 UX、详情变更门禁和相关测试均落地后，重新执行 `make verify`；264 项 Swift Testing（16 个 Suite）、13 项 XCTest、PI/MCP 契约、资源与模型清单、Dev 构建全部通过。
- 随后重新执行 `make xcode-build-store` 和 `make verify-app-store`；Store 组合根构建成功，零模型 Store Bundle 资源门禁通过。`git diff --check`、`make docs-check` 和 `make harness-check` 同样通过。
- 本次验证仍未安装 App、覆盖用户包或重置 TCC；真实桌面浮窗手感、长时故障/Qwen 完整质量矩阵和 Developer ID/公证继续保持为计划中的外部验收项。

## 浮窗桌面验收交由人工

- 按用户决定，本轮停止实际桌面点击验收，不把隔离脚本运行或测试模式状态冒充为目标安装包的视觉/交互通过。
- 保留 `specs/2026-09-01-media-import-background-journey.md` 与 `scripts/acceptance_media_import_desktop.sh`，供后续在没有其他 Woice 进程的目标桌面执行；人工重点确认“关闭并后台继续”后窗口消失、工作台可继续操作且任务不被取消。
- 本轮没有安装新包、覆盖用户包、重置 TCC 或接触正式素材；MRQ-UX-01 的目标桌面手感仍为人工待办。

## 按用户要求跳过实际桌面验收后的最终门禁

- 按用户要求不再自动点击目标安装包，不把隔离测试模式或脚本状态冒充为真实桌面验收；浮窗关闭后的视觉手感与后续操作继续由用户人肉确认。
- `make verify-app-store` 在申请外部缓存访问后通过：Xcode Store 组合根 `BUILD SUCCEEDED`，`verify-xcode-store-bundle`、8 项本地 Store 资源测试和 `verify-app-store` 均通过；Store 包仍为本地检查，不代表 Apple Archive、审核或公证通过。
- `make docs-check`、`make harness-check`、`git diff --check` 与 `zsh -n scripts/acceptance_media_import_desktop.sh` 通过；本轮没有安装 App、覆盖用户包、重置 TCC 或触碰正式素材。

## Qwen 空信号严格门禁与 300 秒复验

- 新增 `specs/2026-09-01-model-benchmark-signal-gates.md`：静音/噪声只有明确的 `empty_result` 才算预期无文字；非空幻觉、模型加载/音频异常和语音类别空输出仍失败，不以放宽门禁伪造通过。
- Qwen Provider 现在把单个无文字 chunk（包括背景噪声）当作长录音中的 no-op；若整段都没有文字，仍返回 `LocalASRError.emptyResult`，不创建 ready Transcript Artifact。新增 XCTest 覆盖空 chunk 与质量拒绝分支。
- Qwen-only 五类 300 秒严格基准通过：`build/model-benchmark-qwen-only-strict-300-20260901.json`；最大 RTF `0.0626`，峰值 RSS `893,648,896` bytes，语音类别非空，静音/噪声为 `empty_result`。
- 重新运行多模型 30 秒严格矩阵后仍按预期失败：WhisperKit Large/Tiny 在静音/噪声返回非空，Large 中文样本空输出；报告 `build/model-benchmark-all-models-strict-20260901.json` 保留问题证据，未把 Qwen-only 通过扩大成全模型通过。
- 夹具生成器在当前环境的 `say` 输出为零字节，已正确 fail-closed；300 秒输入由已存在的 30 秒公开夹具用 FFmpeg 循环生成并逐文件核验时长，不读取用户素材。未安装 App、覆盖用户包、重置 TCC 或发布 Catalog。

## Qwen 门禁修复后的工程回归

- `make test` 通过：265 项 Swift Testing（16 个 Suite）与 14 项 XCTest；新增空信号严格判定和 Qwen 无文字 chunk 回归均通过。
- `make verify` 通过，包含资源/许可证/模型清单、PI/MCP 契约、Dev 构建与文档门禁；随后 `make verify-app-store` 通过，Store 组合根 `BUILD SUCCEEDED`、零模型 Bundle 资源检查和本地 Store 包检查均通过。
- `make lint`、`make docs-check`、`make harness-check`、`git diff --check` 与三份相关脚本语法检查通过。
- 真实桌面/TCC 验收仍按用户要求跳过；本轮没有安装或覆盖用户 App、重置 TCC、提交或推送。

## 旧数据库迁移完整性回归

- 新增 `sqliteMigrationPreservesAudioDigestAndArtifactCount`：用 v5 数据库副本、导入原件字节和一个 Transcript Artifact 验证 v5→v6 摘要投影迁移后录音数量、原件 SHA-256、原件字节数、Artifact 数量/ID 与激活版本均保持不变。
- 定向测试通过；该自动夹具补齐 `MRQ-TAC-031` 的数据库副本完整性证据，但不替代用户真实数据库覆盖升级和桌面视觉确认。

## 最新源码回归与发行包门禁

- `make test` 通过：266 项 Swift Testing（16 个 Suite）与 15 项 XCTest；新增数据库迁移完整性回归、多语言 byte-level 回归与 Qwen 空信号门禁均通过。
- `make verify` 通过：文档/Harness、资源与许可证、PI/MCP 契约、Dev 构建及完整测试保持通过。
- `make verify-app-store` 通过：Store 组合根 `BUILD SUCCEEDED`，零模型 Bundle 资源门禁和本地 Store 包检查通过；这不是 Apple Archive、审核、公证或正式签名结论。
- `make lint`、`make docs-check`、`make harness-check`、`git diff --check` 与相关脚本语法检查通过；新增 `make model-benchmark-qwen-only-strict` 入口已做干运行校验，并指向固定 Qwen 0.6B pack。
- 按用户要求跳过真实桌面点击验收；本轮没有安装/覆盖用户 App、重置 TCC 或接触正式素材。真实桌面、TCC、长时故障、完整 Qwen 准入和发行签名仍由人工/外部条件决定。

## Qwen 60 分钟长时压力矩阵

- 使用现有公开 30 秒夹具循环生成隔离输入，五类（中文、英文、中英混合、静音、噪声）各核验为 `3600.000000` 秒；没有读取正式素材。
- Qwen-only 严格执行通过：`WOICE_BENCHMARK_MIN_DURATION_SECONDS=3600 ./scripts/run_model_benchmark.sh --strict ...`，报告为 `build/model-benchmark-qwen-only-strict-60m-20260901.json`；最大 RTF `0.0606254906`，峰值 RSS `898,056,192` bytes，缺失类别为空。
- 语音类别均非空且无错误，静音/噪声均为 `empty_result`；长时输入是重复夹具且无可信逐字参考，因此 WER 为空，只作为长时稳定性/资源压力证据，不解锁真实内容准确率或正式 Qwen Catalog。

## Qwen 多语言 byte-level 回归

- 新增 `QwenOutputParserTests.testRepairsJapaneseKoreanAndEmojiByteSequences`，覆盖日文、韩文、中文和 emoji 的 GPT-2 byte-level UTF-8 连续解码；12 项 Qwen parser XCTest 全部通过。
- 该回归只证明 Provider 边界的字节修复与合法 Unicode 保留，不替代 Qwen 模型对真实语料的识别准确率；正式 Catalog 仍保持锁定。

## Qwen 官方参考样本对照与可重复夹具

- 根据 Qwen 官方示例提供的固定音频 URL 与逐字文本，下载公开中文/英文样本到隔离目录；官方示例列出的中文参考为“甚至出现交易几乎停滞的情况。”，英文参考为完整公开句子，未使用用户素材。
- 固定 Qwen pack 单独运行 Woice 基准：中文 WER `0`、英文 WER `0.0263157895`，均非空且无错误；报告为 `build/model-benchmark-qwen-only-official-reference-20260901.json`。
- 新增 `scripts/fetch_qwen_reference_fixtures.sh` 与 `make model-benchmark-qwen-reference-fixture`：固定 HTTPS URL、逐文件 SHA-256 和参考文本，下载/摘要失败会清理 partial 并 fail-closed；脚本在 `/private/tmp/woice-qwen-reference-script-20260901` 实际下载并复核摘要通过。
- `make model-benchmark-qwen-official-reference` 已实际通过，提供一条可重复的“准备官方夹具 → 构建 Store 依赖 → 固定 Qwen pack 对照”入口；最新报告为 `build/model-benchmark-qwen-only-official-reference-from-make-20260901.json`，中文/英文 WER 仍为 `0`/`0.0263`。未解锁正式 Catalog，完整混合/真实会议质量仍待。

## 用户要求跳过实际验收后的最终自动复核

- 按用户要求不启动 App、不覆盖安装、不重置 TCC；真实桌面、长时故障和会议内容质量由用户人肉验收。
- `make verify` 在提升权限下通过：PI Connector 7/7、MCP 2/2、267 项 Swift Testing、15 项 XCTest、资源、许可证、文档、Harness、Dev 构建均通过；沙箱初跑的 Unix Socket `EPERM` 已确认是环境限制。
- `make verify-app-store` 在提升权限下通过：Store 组合根 `BUILD SUCCEEDED`，零模型 Bundle 校验通过；该结果不代表正式签名、公证、TestFlight 或审核。
- 官方参考夹具脚本重新执行并复核 SHA-256：中文 `46dbc998c9d1d48111267c40741dd3200f2e5bcf4075f8c4c97f4451160dce50`、英文 `f9b4440ac8393e47c14a6240e9739dea09b645bb1592b8f2dd48feb9666cea7f`；`zsh -n`、`make docs-check`、`make harness-check`、`make lint` 和 `git diff --check` 通过。

## 模型库存扫描隔离

- 新增 `specs/2026-09-01-model-inventory-isolation.md`：损坏或半安装模型版本按版本隔离，不能隐藏其它已验证模型；安装、目录读取和 Runtime 构造仍严格 fail-closed。
- `ModelPackStore.inventory()` 现在对 bundled/downloaded 版本逐包校验并跳过坏版本；新增 `modelPackInventorySkipsCorruptVersion` 回归，验证有效版本、current 指针和损坏版本共存时有效模型仍可见。
- 定向 Swift 回归通过；真实模型页视觉/TCC 仍按用户要求交由人工。

## App Store 0.1.4 Build 6 上传

- 用户确认三声道麦克风 AAC 修复的手动测试通过后，按 `specs/2026-09-01-app-store-build6-submission.md` 启动正式 Store 发布链；版本冻结为 `0.1.4 (Build 6)`，Store 包不携带模型权重。
- `make verify` 与 `make verify-app-store` 通过：269 项 Swift Testing、18 项 XCTest、PI 7 项、MCP 2 项、文档/Harness/lint、双架构 Store 构建及零模型资源门禁均成功。受限环境首次因临时 Unix Socket 返回 `listen EPERM`，在具备本机权限的同一命令下复跑通过，未归因于产品代码。
- 正式生成 `build/Woice-Store-0.1.4-build6.xcarchive`；深度签名校验、版本/Build、`com.water.woice`、Store Channel、`x86_64 + arm64` 和零模型目录均通过。
- App Store Connect 上传明确返回 `Upload succeeded`、`Uploaded Woice-Store` 与 `Uploaded package is processing`。该状态只表示 Apple 已接收并处理包，不代表 Build 已可选、已提交审核或已上架。
- App Store Connect 当前 macOS 版本页仍为 `0.1.3`，截图为 `0/10`，且隐私政策 URL、审核联系人、版权主体、描述/关键词/审核备注等尚未最终填写。首次发布已保持手动发布；资料完整并完成动作前确认之前，不点击“添加以供审核”或“提交以供审核”。
- 已核验公开的 Woice 产品页可作为营销 URL，公开 GitHub Issues 可作为支持 URL；独立隐私政策页尚不存在。桌面当前处于锁定状态，无法继续生成真实无隐私截图或复核 Apple 的 Build 6 处理结果。
- 产品负责人确认营销 URL 使用 `https://wattter.cn/woice`，并授权发布 GitHub 隐私政策、填写商店资料和继续正式提交链。该 URL 当前可访问但返回 Wattter 首页，后续官网应补独立 Woice 路由；本次按指定地址填写，不将首页内容误记为独立产品页。

## 本机 Woice 安装版本收敛

- 安装前盘点确认 `/Applications` 与用户应用目录只有 `/Applications/Woice (Dev).app`，原版本为 `0.1.4 (Build 5)`；项目中的 Build 6 App 均为构建产物，不冒充已安装版本。
- 使用登录钥匙串中的 Apple Development 身份执行 `make install`，将本机 Dev Channel 覆盖升级为 `0.1.4 (Build 6)`；完整钥匙串权限下 `codesign --verify --deep --strict` 通过，Bundle ID 保持 `com.woice.app`。
- 项目清理脚本移除可重建的 `build/Woice (Dev).app`、`build/Woice-Store.app` 及两个 Derived Products App；当前安装目录只剩最新 `/Applications/Woice (Dev).app`。
- 保留 `build/Woice-Store-0.1.4-build6.xcarchive`、正式版与 Dev 版 Application Support 数据；未删除录音、数据库、模型、设置或待审核 Archive。
