# 2026-09-01

## TestFlight 0.1.4 Build 5 上传

- 发布内容：包含工作台侧栏恢复、模型库存去重、首次模型提示、内存推荐和 Dev/Store 隔离；Build 从 4 升至 5，Store 包继续为 `Woice` / `com.water.woice` / Store Channel。
- 包体边界：Archive 为 arm64 + x86_64；`DistributionManifest` 的随包模型列表为空，App Resources 不含 `Models` 或 `BundledModels`。
- 自动门禁：完整 `make verify` 通过 Swift 237 项、PI 7 项、MCP 2 项；首次受限运行因 Unix Socket `EPERM` 失败，切换到本机环境原样复跑通过。
- 签名说明：Xcode 自动签名 Archive 与 App Store 导出成功。本机 `pkgutil` 仍将 Installer 证书链报告为不受信，与上一 Build 的钥匙串状态一致；未把本机结论冒充为 Apple 校验结果。
- 上传结果：`xcodebuild -exportArchive` 完成 App Store Connect 分析和 100% 上传，明确返回 `Upload succeeded`、`Uploaded Woice-Store`；Apple 当前显示 `Uploaded package is processing`，尚不宣称 TestFlight 已可安装或 App Store 已发布。
- Catalog 边界：既有 Ed25519 发布私钥仍缺失，没有生成替代信任根。Build 5 使用当前线上已签名 Catalog；Qwen 目标档位不可下载时安全回退到 Catalog 中可下载的模型。
- 清理：完整验证产生的顶层与 Derived Products Dev App 已删除；Build 4 旧 Archive 已带时间戳移入废纸篓，只保留 Build 5 当前 Archive，不触碰 `/Applications/Woice (Dev).app` 或用户数据。
