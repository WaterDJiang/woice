# 2026-08-25 开发记录

## 导入转写浮窗可关闭与后台任务体验

- 导入 Sheet 现在通过父级 `isShowingMediaImport` 显式控制呈现，避免仅调用环境 dismiss 后浮窗残留。
- 点击“转文字”后立即关闭浮窗并切到“处理任务”工作区；本机转写、外部确认和任务排队均继续使用既有持久化链路。
- 文件已保存后关闭动作按状态显示“关闭”或“后台运行”，明确关闭不会取消任务；原件和任务状态不变。
- `make test`：217 项 / 14 个 Suite 通过；`make lint`、`make docs-check`、`make harness-check`、`git diff --check` 通过。
- 待用户在已安装 App 中复验：导入文件后点击“转文字”，确认浮窗立即退出、可导航到其他工作区，处理任务仍持续更新。

## 实时文字、顶部面板收起与导入页优化

- 实时转写后台状态原先没有 UI 消费者；新增共用 `LiveTranscriptPreviewCard`，工作台与菜单栏 Popover 在麦克风录音时显示准备、监听、partial transcript 和不可用状态。
- “录音时显示实时文字”移到“录音与转写”首层，明确工作台/顶部面板位置、本机处理和不覆盖最终原文。
- 菜单栏 Popover 增加应用内面板外点击监听，面板内和 Woice 状态按钮保留原语义；切换到其他应用时使用 AppKit 失去焦点通知收起，不长期监听系统级鼠标事件，关闭后清理监听和通知。
- 导入 Sheet 收紧为单一投放区，支持按钮选择和单文件拖放，同屏显示支持格式与本机保存边界；导入后立即转文字的既有链路不变。
- 新增 3 项状态投影/收起策略测试；`swift test --quiet` 通过 217 项 / 14 个 Suite，`make lint`、`make docs-check`、`make harness-check`、`make xcode-build-store` 通过。

## 使用已安装 Apple Development 证书的稳定 Qwen 包

- 复核登录钥匙串后确认本机已有可用 Apple Development 身份；身份名称、指纹与 Team ID 仅在本机验证，不写入仓库。此前 Offline 安装包使用的是显式默认 `-`，因此为 Ad Hoc。
- 使用本机钥匙串身份重新打包带 Qwen3-ASR 0.6B 4-bit 的 Offline App，并覆盖安装到 `/Applications/Woice.app`。Bundle ID、签名链、Qwen 文件 SHA-256、MLX Metal Bundle 与严格验签通过；个人签名材料不进入 Release 或仓库。
- 现有用户数据未修改；从 Ad Hoc 切换到稳定身份可能需要对当前安装包重新授权一次，后续同一 Team ID/Bundle ID/权限声明的覆盖更新才具备 TCC 连续性条件。
- `WOICE_STABLE_SIGNING_IDENTITY="$WOICE_LOCAL_SIGNING_IDENTITY" WOICE_RUN_STABLE_UPGRADE=1 make acceptance-stable-upgrade` A/B 门禁通过：Build `2026082501 → 2026082502`、Bundle ID、Team ID、权限声明和签名要求一致；TCC 实际连续性仍需用户手测。
- 旧 Ad Hoc App 可恢复备份：`/private/tmp/Woice-before-stable-qwen-20260825-094151.app`；稳定签名 A/B 证据：`/private/tmp/woice-stable-ab-qwen-20260825`。

## 模型转写页可见切换与 Qwen Offline 本机安装

- 修复“模型与转写”页只显示当前摘要、已安装模型藏在高级区的问题：首屏现在直接显示所有已验证本机模型的名称、版本、来源和数量，并可通过菜单切换 Tiny、Large-v3、Qwen3-ASR 等已安装版本。
- 切换动作使用实际 `ModelRuntimeRegistry` admission 和模型加载结果；切换中显示进度，失败保持原模型并给出反馈；成功后同步 AppState 与设置草稿，保存其他服务设置不会回退模型选择。
- `swift test --quiet`：214 项 / 14 个 Suite 通过；`swift format lint --recursive Sources Tests` 通过。
- 最终 `make xcode-build-store` 通过，Store 组合根在 `WOICE_APP_STORE` 下编译模型选择页，并通过 `verify-xcode-store-bundle`。
- `WOICE_CODESIGN_IDENTITY="$WOICE_LOCAL_SIGNING_IDENTITY" WOICE_OFFLINE_MODEL_ROOT=/private/tmp/woice-qwen3-asr-0.6b-4bit-package make package-offline` 通过；Offline 包含 Qwen3-ASR 0.6B 4-bit 固定 revision `313d850181767edf09f00a9c289becca70e58cd0`、5 个模型文件、Apache-2.0 provenance 和 `mlx-swift_Cmlx.bundle`，逐文件 SHA-256 与 manifest 一致，`codesign --verify --deep --strict` 通过。
- 已将最终 `build/Woice-Offline.app` 覆盖安装到 `/Applications/Woice.app`，当前 Bundle 为 `0.1.2 (Build 1)`、`offline`；上一版 `/Applications/Woice.app` 可恢复备份保存在 `/private/tmp/Woice-before-qwen-ui-20260825-093106.app`，`~/Library/Application Support/Woice` 未修改。
- 待用户在已安装 App 中手测：打开“模型与转写”页确认 Qwen 菜单可见，切换后用已有素材重转写并检查错误/进度反馈；Qwen 准确率、长会性能和正式签名 Catalog 仍不宣称完成。

## Apache-2.0 Runtime 授权后的 WCL-07 收口更新

- 用户明确允许引入 Apache-2.0 原生 Swift/MLX Runtime；`vfasky/qwen3-asr-swift` 固定到 `4824c95e1e4624200405d639fb4ebe10f93f1075`，`mlx-swift` 固定到 `0.31.6` 及 `Package.resolved` 中的传递依赖。
- Qwen3-ASR-0.6B-4bit 派生模型固定到 `313d850181767edf09f00a9c289becca70e58cd0`；5 个模型文件总计 `712699126` bytes，逐文件大小/SHA-256 与 manifest 一致，来源、上游 revision、转换工具、Apache-2.0 和 SBOM 已记录。
- Qwen 原生 Provider 已进入现有分段/Transcript 链路；使用固定模型完成原生加载与非空转写冒烟。实现不启动 Python、Shell、HTTP 端口或外部进程。
- Xcode `Release-Direct` / `Release-AppStore` 配置负责 MLX `default.metallib`，打包时将 `mlx-swift_Cmlx.bundle` 放入 App Resources；Store 模型包仍只接受数据文件并拒绝可执行内容。
- 通过：`swift test --quiet`（214 项 / 14 个 Suite）、Qwen 原生冒烟、`make xcode-build-store`、`make package-core`、`make package-store`、`verify-xcode-store-bundle`、`python3 scripts/verify_app_store.py --app build/Woice-Store.app`、`make docs-check`、`make harness-check`。
- 仍待：固定音频准确率、300 秒/长会议性能与峰值内存矩阵、签名 Catalog 条目、真实录音与 Store 签名/沙盒手测；因此 Qwen 尚未进入正式推荐或发布 Catalog。
- 最终回归：格式化告警已修正；`make lint`、`git diff --check`、`make docs-check`、`make harness-check` 通过；格式化后 `swift test --quiet` 仍为 214 项 / 14 个 Suite 通过。

## WCL-07 一键模型安装基础链路与 Store 数据包门禁

- 三个入口共用 `ModelInstallCoordinator` 和 `ModelInstallIntent`；模型下载任务新增 `preflighting`、`installing`、`activating` 状态，并在 SQLite 中持久化入口与素材 continuation。
- 模型激活成功后扫描并恢复 `waitingForModel` 的本机转写任务；旧录音、旧原文和当前模型在失败时保持不变。
- `ModelRuntimeRegistry` 成为 Provider 到内置 Runtime 的唯一 admission 边界；健康状态只依据已验证模型库存，不再仅凭过期 Provider ID 判断本机模型可用。
- Store-compatible 模型清单要求 provenance；安装时拒绝清单外文件、符号链接、非普通文件、可执行权限、脚本扩展、Mach-O 文件头等内容。
- Qwen3-ASR 已登记为 fail-closed Provider；本段记录的是用户授权前的阻塞状态，授权后的实现与验证见上方收口更新。

## WCL-07 静态验证与阻塞边界

- 通过：`make docs-check`、`make harness-check`、变更 Swift 文件 `swift format lint`、`swiftc -frontend -parse`、`git diff --check`。
- 授权前未执行的完整编译/测试不在本段重复宣称；授权后的实际命令与结果见上方“授权后的 WCL-07 收口更新”。
- 下一步：完成性能/内存矩阵、签名 Catalog 和真实录音/Store 沙盒手测后，再决定是否进入下一版发布。

## 本机模型一键安装与 App Store 兼容规划

- 新增 `specs/2026-08-25-one-click-model-installation-and-store-compatibility.md`，冻结工作台、待转写素材和设置页三个入口共用的一次点击安装流程。
- 用户点击后自动完成预检、下载、校验、安装、激活和等待转写恢复；不增加确认 Sheet、Endpoint 配置、文件夹选择或许可证勾选。
- Qwen3-ASR-0.6B 只有在固定官方 revision、Apache-2.0 ADR、派生链/SHA/NOTICE/SBOM、原生 in-process Runtime 和性能门禁通过后才进入正式 Catalog。
- Store 模型包只允许权重、Tokenizer、配置与许可证数据；禁止下载或执行 Python、脚本、dylib、bundle 和其他运行时。
- 当前技术开发计划新增 WCL-07 与 WCL-TAC-020～027；真实首次用户感受和 App Review 实际反馈只作非开发提醒。

## 双音源、压缩存储与长素材详情

- 工作台和菜单栏增加“麦克风”“电脑声音”两个独立来源按钮，默认双开；任一可关闭，两者全关时禁止开始并显示“请至少开启一个音源”。录音中来源锁定。
- 麦克风单轨只启动 `AVAudioEngine`；电脑声音单轨只启动 ScreenCaptureKit；双开保留两条原轨并生成会议合成文件。
- 新录音改为 AAC/M4A：麦克风 64 kbps、电脑声音 128 kbps、会议合成 64 kbps；ASR 需要时再生成 16 kHz 工作文件。旧 WAV/CAF 不转码、不覆盖。
- 会话 Journal 增加 `captureMicrophone` 并兼容旧数据；异常恢复系统音频单轨时不再伪造麦克风或会议合成。
- 详情页取消打开时同步预加载全部音轨，改为单一播放器和三个音轨选择按钮；点击播放才加载当前文件。原文与时间轴使用固定高度独立滚动，时间轴使用 `LazyVStack`。
- SwiftUI UI Pattern 约束用于原生按钮状态、键盘/VoiceOver 语义和懒加载滚动结构。

## 验证

- `make acceptance-core`：通过；覆盖真实麦克风、Loopback/自定义/本机 ASR、时间戳、时长和播放元数据。
- `make verify`：207 项 Swift 测试 / 14 个 Suite 通过；PI 6 项、MCP 2 项、lint、生产构建和 Core 打包通过。
- `make store-capability-check`：185 项 Swift 测试 / 11 个 Suite 通过。
- `make xcode-build-store`：`Woice-Store / Release-AppStore` 无签名 Universal 构建和 Store Bundle 资源校验通过。
- `make install`：最终官网 Core 包已覆盖到 `/Applications/Woice.app`；Bundle `0.1.0 (Build 1)` 严格验签通过，安装前后主二进制 SHA-256 均为 `c3857c4cff9777558bf4985df4ce8b0aefc0ee47a693e6f26e3f83ef1573df1f`，启动后进程与“Woice 工作台”窗口可见。
- 真实会议声源组合与 50 分钟以上素材的主观打开速度由用户在当前安装包人工复验。

## 录音控制区视觉层级

- 按 `ui-ux-pro-max` 的主次动作层级与 `swiftui-ui-patterns` 的原生控件约束，菜单栏 Popover 将“录音来源”整理为独立中性设置区；麦克风和电脑声音用图标、文字、细边框和勾选表达状态，不再与主动作同时铺满蓝色。
- 开始录音成为唯一蓝色主动作，图标从 `mic.fill` 改为 `record.circle`；结束录音仍使用红色 `stop.fill`，避免“麦克风来源”和“开始录音”图标重复。
- 用户复核认为工作台顶部动作仍然拥挤，最终移除悬空的“导入音视频”、右上角麦克风/电脑声音/录音三个按钮，以及空闲时悬在中间的处理状态胶囊。
- 导入与开始录音仍保留在素材库空状态；快速音源选择和录音保留在菜单栏 Popover，不在工作台顶部重复。`RecordingSourceControl` 只服务菜单栏，四种来源组合使用同一摘要，并保留录音中锁定来源、Help、VoiceOver 和 Reduce Motion 语义。

## 录音控制区验证

- `swift test --filter RecordingSourceSelectionTests`：4 项通过，覆盖默认双开、双关 fail-closed 与四种来源摘要。
- `make verify`：208 项 Swift 测试 / 14 个 Suite、PI 6 项、MCP 2 项、lint、生产构建和 Core 打包通过。
- `make store-capability-check`：186 项 Swift 测试 / 11 个 Suite 通过；`make xcode-build-store` 正式 Store 组合根和 Bundle 门禁通过。
- 用户复核后的 `make verify`：208 项 Swift 测试 / 14 个 Suite、PI/MCP、lint、生产构建和 Core 打包再次通过。
- `make install`：最终清爽顶部版本覆盖安装到 `/Applications/Woice.app`；严格签名通过，安装包与构建包主二进制 SHA-256 均为 `fdfec53bc629886c1bf64a5f833bc8ae8d0c0915ee50e6376c6724e27bbdd615`。进程存活且“Woice 工作台”窗口可见；未修改 `~/Library/Application Support/Woice` 用户素材。

## Woice v0.1.2 Core GitHub Release

- 提交 `ec3a481` 已推送到 `origin/main`，标签 `v0.1.2` 已推送并指向该提交。
- GitHub Release：[Woice v0.1.2 Core](https://github.com/WaterDJiang/woice/releases/tag/v0.1.2) 已发布为正式 Release（非 Draft、非 Pre-release）。
- 仅上传 Core 资产：`Woice-Core-0.1.2-1-arm64.dmg`，3,973,104 bytes，SHA-256 `114182ac422db046e9fc746581f87eea16529e7bb26212eff039aa6744071021`；`SHA256SUMS.txt` 为 122 bytes，远端 digest 与本地一致。
- Core Bundle 版本为 `0.1.2 (Build 1)`，二进制仅 `arm64`；`codesign --verify --deep --strict` 与 `hdiutil verify` 通过。
- 该版本采用 ad hoc 签名，未进行 Developer ID 公证；Core 不内置模型，Offline 资产未随本 Release 上传。
- 发布前 `make package-dmg-core` 内含的 `make verify` 通过：208 项 Swift 测试 / 14 个 Suite、PI 6 项、MCP 2 项、lint、生产构建和 Core 打包均通过。
